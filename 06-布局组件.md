# 布局组件

## 目录

- [ZoehisAside](#zoehisaside)
- [ZoehisBasePageLayout](#zoehisbasepagelayout)
- [ZoehisColItem](#zoehiscolitem)
- [ZoehisContainer](#zoehiscontainer)
- [ZoehisFooter](#zoehisfooter)
- [ZoehisHeader](#zoehisheader)
- [zoehisMain](#zoehismain)
- [ZoehisPartition](#zoehispartition)
- [ZoehisRowSearch](#zoehisrowsearch)

---

## ZoehisAside

### 概述
ZoehisAside 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisAside from '@zoesoft.com.cn/his-component-vue/packages/aside';
Vue.use(ZoehisAside);
```

### 基础用法
```vue
<zoehis-aside></zoehis-aside>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-aside
  width="200px"
  direction="right">
</zoehis-aside>
```

### 常用方法使用
```vue
<template>
  <zoehis-aside ref="myRef"></zoehis-aside>
  <zoehis-button @clickenter="$refs.myRef.dragmove()">调用 dragmove 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| width | String | 200px |  |
| direction | String | right | 位置 aside仅支持left与right |
| partitionIcons | Array | - | 图标 |

### ② 事件说明（Events）
该组件未触发自定义事件。

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| dragmove | moveLen |  |
| iconClick | iconStatus |  |

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisBasePageLayout

### 概述
基础页面布局容器

### 引入方式
```js
import Vue from 'vue';
import ZoehisBasePageLayout from '@zoesoft.com.cn/his-component-vue/packages/page-layout';
Vue.use(ZoehisBasePageLayout);
```

### 基础用法
```vue
<zoehis-base-page-layout></zoehis-base-page-layout>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-base-page-layout
  type="1">
</zoehis-base-page-layout>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| type | String / Number | 1 |  |
| headerStyle | Object / String | - |  |
| leftAsideStyle | Object / String | - |  |
| rightAsideStyle | Object / String | - |  |
| mainStyle | Object / String | - |  |
| footerStyle | Object / String | - |  |
| topMainStyle | Object / String | - |  |
| bottomMainStyle | Object / String | - |  |
| singleLeftAsideFlag | Boolean | false |  |
| singleRightAsideFlag | Boolean | false |  |
| mainSplitFlag | Boolean | false | 主区域是否进行分隔 |
| mainPartionAttr | Object | - | 主区域区域分块组件支持属性 |

### ② 事件说明（Events）
该组件未触发自定义事件。

### ③ 方法说明（Methods）
该组件未定义方法。

### Slots（插槽）
| 插槽名 | 作用域参数 | 说明 |
|--------|-----------|------|
| header | - |  |
| left-aside | - |  |
| main | - |  |
| top-main | - |  |
| bottom-main | - |  |
| right-aside | - |  |
| footer | - |  |

---

## ZoehisColItem

### 概述
ZoehisColItem 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisColItem from '@zoesoft.com.cn/his-component-vue/packages/col-item';
Vue.use(ZoehisColItem);
```

### 基础用法
```vue
<zoehis-col-item></zoehis-col-item>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-col-item>
</zoehis-col-item>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| label | String | - |  |
| labelSuffix | String | - |  |
| labelWidth | String | - |  |
| labelWidthFixed | Boolean | false | 内部使用，是否强制限制label宽度 |
| labelStyle | Object | - | 自定义label样式 |

### ② 事件说明（Events）
该组件未触发自定义事件。

### ③ 方法说明（Methods）
该组件未定义方法。

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisContainer

### 概述
ZoehisContainer 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisContainer from '@zoesoft.com.cn/his-component-vue/packages/container';
Vue.use(ZoehisContainer);
```

### 基础用法
```vue
<zoehis-container></zoehis-container>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-container>
</zoehis-container>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| direction | String | - |  |

### ② 事件说明（Events）
该组件未触发自定义事件。

### ③ 方法说明（Methods）
该组件未定义方法。

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisFooter

### 概述
ZoehisFooter 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisFooter from '@zoesoft.com.cn/his-component-vue/packages/footer';
Vue.use(ZoehisFooter);
```

### 基础用法
```vue
<zoehis-footer></zoehis-footer>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-footer
  height="50px"
  direction="top">
</zoehis-footer>
```

### 常用方法使用
```vue
<template>
  <zoehis-footer ref="myRef"></zoehis-footer>
  <zoehis-button @clickenter="$refs.myRef.dragmove()">调用 dragmove 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| height | String | 50px |  |
| direction | String | top | 位置 footer仅支持top |
| partitionIcons | Array | - | 图标 |

### ② 事件说明（Events）
该组件未触发自定义事件。

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| dragmove | moveLen |  |
| iconClick | iconStatus |  |

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisHeader

### 概述
ZoehisHeader 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisHeader from '@zoesoft.com.cn/his-component-vue/packages/header';
Vue.use(ZoehisHeader);
```

### 基础用法
```vue
<zoehis-header></zoehis-header>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-header
  height="40px"
  direction="bottom">
</zoehis-header>
```

### 常用方法使用
```vue
<template>
  <zoehis-header ref="myRef"></zoehis-header>
  <zoehis-button @clickenter="$refs.myRef.dragmove()">调用 dragmove 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| height | String | 40px |  |
| direction | String | bottom | 位置 header仅支持bottom |

### ② 事件说明（Events）
该组件未触发自定义事件。

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| dragmove | moveLen |  |
| iconClick | iconStatus |  |

### Slots（插槽）
该组件未定义插槽。

---

## zoehisMain

### 概述
ZoehisMain 组件

### 引入方式
```js
import Vue from 'vue';
import zoehisMain from '@zoesoft.com.cn/his-component-vue/packages/main';
Vue.use(zoehisMain);
```

### 基础用法
```vue
<zoehis-main></zoehis-main>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-main>
</zoehis-main>
```

### 常用方法使用
```vue
<template>
  <zoehis-main ref="myRef"></zoehis-main>
  <zoehis-button @clickenter="$refs.myRef.dragmove()">调用 dragmove 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| partitionIcons | Array | - | 图标 |

### ② 事件说明（Events）
该组件未触发自定义事件。

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| dragmove | moveLen |  |
| iconClick | iconStatus |  |

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisPartition

### 概述
ZoehisPartition 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisPartition from '@zoesoft.com.cn/his-component-vue/packages/partition';
Vue.use(ZoehisPartition);
```

### 基础用法
```vue
<zoehis-partition></zoehis-partition>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-partition>
</zoehis-partition>
```

### 常用事件使用
```vue
<template>
  <zoehis-partition @input="handleEvent">
  </zoehis-partition>
</template>
<script>
export default {
  methods: {
    handleEvent(val) {
      console.log('input 事件触发', val);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-partition ref="myRef"></zoehis-partition>
  <zoehis-button @clickenter="$refs.myRef.toggleEn()">调用 toggleEn 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| normalSize | Array | - | 默认时的尺寸大小 |
| activeSize | Array | - | 响应时的尺寸大小 |
| verticalArrow | Array | - | 竖向时的图标 |
| horizontalArrow | Array | - | 横向时的图标 |
| mode | String | vertical | 类型 横向还是竖向 vertical / horizontal |
| showArrow | Boolean | true | 是否要显示按钮 默认显示 |
| value | Boolean | false | 点击按钮的响应值 |
| lineDirection | Boolean | false | 分割线样式的区别 默认false |
| draggable | Boolean | false | 是否支持拖动 默认支持拖动 |
| minLeft | Number | 30 | 最小允许向左/向上 拖动距离 左边界/上边界多少距离 |
| minRight | Number | 35 | 最小允许向右/向下 拖动距离 右边/下边界多少距离 |
| iconCls | String | - | 自定义按钮class名称 |
| lineClass | String | - |  |
| uiType | String | default |  |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| input | val |  |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| toggleEn | - |  |

### Slots（插槽）
| 插槽名 | 作用域参数 | 说明 |
|--------|-----------|------|
| left | - |  |
| right | - |  |

---

## ZoehisRowSearch

### 概述
ZoehisRow 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisRowSearch from '@zoesoft.com.cn/his-component-vue/packages/row';
Vue.use(ZoehisRowSearch);
```

### 基础用法
```vue
<zoehis-row-search></zoehis-row-search>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-row-search>
</zoehis-row-search>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| searchBtnClassName | String | - | 自定义按钮组类名 |

### ② 事件说明（Events）
该组件未触发自定义事件。

### ③ 方法说明（Methods）
该组件未定义方法。

### Slots（插槽）
| 插槽名 | 作用域参数 | 说明 |
|--------|-----------|------|
| condition | - |  |
| button | - |  |

---

