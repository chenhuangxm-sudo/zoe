# 表单组件

## 目录

- [ZoehisAutocomplete（自动补全）](#zoehisautocomplete)
- [ZoehisCheckbox（复选框）](#zoehischeckbox)
- [ZoehisColorPicker（颜色选择器）](#zoehiscolorpicker)
- [ZoehisInput（输入框）](#zoehisinput)
- [zoehisInputNumber（数字输入框）](#zoehisinputnumber)
- [ZoehisManuallyInput（手动输入框）](#zoehismanuallyinput)
- [ZoehisMultipleSelect（多选下拉）](#zoehismultipleselect)
- [ZoehisRadio（单选框）](#zoehisradio)
- [ZoehisSelect（下拉选择）](#zoehisselect)
- [ZoehisSlider（滑块）](#zoehisslider)
- [ZoehisSwitch（开关）](#zoehisswitch)
- [ZoehisUpload（上传）](#zoehisupload)

---

## ZoehisAutocomplete（自动补全）

### 概述


### 引入方式
```js
import Vue from 'vue';
import ZoehisAutocomplete from '@zoesoft.com.cn/his-component-vue/packages/autocomplete';
Vue.use(ZoehisAutocomplete);
```

### 基础用法
```vue
<zoehis-autocomplete></zoehis-autocomplete>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-autocomplete
  :multiple="true"
  width="115"
  dropdownMaxWidth="800">
</zoehis-autocomplete>
```

### 常用事件使用
```vue
<template>
  <zoehis-autocomplete @scrollTop="handleEvent">
  </zoehis-autocomplete>
</template>
<script>
export default {
  methods: {
    handleEvent(val) {
      console.log('scrollTop 事件触发', val);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-autocomplete ref="myRef"></zoehis-autocomplete>
  <zoehis-button @clickenter="$refs.myRef.slotItem()">调用 slotItem 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| multiple | Boolean | false | 多选 |
| width | Number / String | 115 | 文本的宽度 |
| dropdownMaxWidth | Number / String | 800 | 弹窗的最大宽度 |
| triggerOnFocus | Boolean | true | 是否在输入框 focus 时显示建议列表 |
| delayTime | Number | 200 | 获取输入建议的去抖延时 |
| highlightFirstItem | Boolean | false | 是否默认突出显示远程搜索建议中的第一项 |
| preventOverId | String | - | 设置弹出框将要插入的容器（默认插入到body下），该值为ID，每个页面都是唯一的不能重复 |
| dropdownSearch | Boolean | false | 是否显示下拉框的搜索 |
| align | String | left | 设置弹出窗的位置（左对齐、居中对齐、右对齐） left center right |
| itemtext | String | text | 项文本 |
| itemcode | String | id | 项值 |
| selectWhenUnmatched | Boolean | false | 在输入没有任何匹配建议的情况下，按下回车是否触发 select 事件 |
| value | - | - | 绑定值 |
| disabled | Boolean | - | 是否禁用 |
| clickselect | Boolean | false | 点击全选 |
| uiType | String | - | 配置不同的ui样式  默认是带下划线的输入框，rectangle矩形带边框的输入框 |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| scrollTop | wrap | - |
| select | null | 选中时触发 |
| input | text | 值变化时触发 |
| clear | event | 清空时触发 |
| change | value | 值改变时触发 |
| inputnative | value | 原生 input 事件触发 |
| focus | event | 获取焦点时触发 |
| blur | event | 失去焦点时触发 |
| clickicon | event | 点击图标时触发 |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| slotItem | itemcode | - |
| reconSelectData | data,type | - |
| getValue | - | 获取当前值 |
| setValue | - | 设置当前值 |
| getDisabled | - | 获取是否可以编辑的状态 |
| focus | - | 焦点聚集的方法 |
| scrollafter | wrap | } |
| handleKeyEnter | e | 内部事件处理 |
| highlight | index | - |
| handleCompositionstart | event | 内部事件处理 |
| handleCompositionUpdate | event | 内部事件处理 |
| handleCompositionEnd | event | this.handleInput(text); |
| placementChangeDrop | placement | 弹窗的上下位置变了 |
| handlePlacementChange | - | bottom、top互换 |
| getData | queryString, type | } |
| getInput | - | 获取输入框元素 |
| select | item, index | item.checked = !item.checked; |
| handleInput | value, other | this.$emit("input", value); |
| handleClear | event | 内部事件处理 |
| handleChange | value | 内部事件处理 |
| handleInputnative | value | 不聚焦就显示，且没有值时候，清空推荐信息 |
| handleFocus | event | 内部事件处理 |
| handleBlur | event | } |
| handleClickicon | event | 内部事件处理 |
| close | e | 关闭 |
| debInputEvent | value | - |
| debInputnativeEvent | value | - |
| debChangeEvent | value | - |

### Slots（插槽）
| 插槽名 | 作用域参数 | 说明 |
|--------|-----------|------|
| default | - |  |

---

## ZoehisCheckbox（复选框）

### 概述
ZoehisCheckbox 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisCheckbox from '@zoesoft.com.cn/his-component-vue/packages/checkbox';
Vue.use(ZoehisCheckbox);
```

### 基础用法
```vue
<zoehis-checkbox></zoehis-checkbox>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-checkbox
  :validateEvent="true">
</zoehis-checkbox>
```

### 常用事件使用
```vue
<template>
  <zoehis-checkbox @change="handleEvent">
  </zoehis-checkbox>
</template>
<script>
export default {
  methods: {
    handleEvent(val) {
      console.log('change 事件触发', val);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-checkbox ref="myRef"></zoehis-checkbox>
  <zoehis-button @clickenter="$refs.myRef.getDisabled()">调用 getDisabled 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| validateEvent | Boolean | true | 内部使用 |
| value | - | - | 绑定值 |
| label | - | - | 标签文本 |
| name | String | - | 文件字段名 |
| trueflag | String / Boolean / Number | true | 选中值 |
| falseflag | String / Boolean / Number | false | 未选中值 |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| change | val | 值改变时触发 |
| input | curVal | 值变化时触发 |
| inputnative | curVal | 原生 input 事件触发 |
| click | [this.selfModel,event] | 点击时触发 |
| enter | evt | 按下回车时触发 |
| focus | - | 获取焦点时触发 |
| blur | event | 失去焦点时触发 |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| handleClick | - | 内部事件处理 |
| getDisabled | - | 获取是否可以编辑的状态 |
| focus | - | 手动聚焦 |
| handleEnter | evt | 内部事件处理 |
| handleFocus | - | 焦点聚集的方法 |
| handleBlur | event | 失焦事件 |
| addToStore | - | - |

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisColorPicker（颜色选择器）

### 概述
ZoehisColorPicker 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisColorPicker from '@zoesoft.com.cn/his-component-vue/packages/color-picker';
Vue.use(ZoehisColorPicker);
```

### 基础用法
```vue
<zoehis-color-picker></zoehis-color-picker>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-color-picker
  :showAlpha="true"
  colorFormat="hex">
</zoehis-color-picker>
```

### 常用事件使用
```vue
<template>
  <zoehis-color-picker @change="handleEvent">
  </zoehis-color-picker>
</template>
<script>
export default {
  methods: {
    handleEvent(val) {
      console.log('change 事件触发', val);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-color-picker ref="myRef"></zoehis-color-picker>
  <zoehis-button @clickenter="$refs.myRef.clearValue()">调用 clearValue 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| value | String | - | 绑定值 |
| showAlpha | Boolean | true | 是否显示透明度 |
| colorFormat | String | hex | 颜色格式 |
| disabled | Boolean | - | 是否禁用 |
| popperClass | String | - | 弹出类名 |
| commonDefine | Array | - | 常用 |
| defaultDefine | Array | - | 默认颜色 |
| model | String | dialog | 模式，默认 dialog缩略图弹窗，panel面板 |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| change | val | 值改变时触发 |
| active-change | val | - |
| input | null | 值变化时触发 |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| handleTrigger | - | 内部事件处理 |
| clearValue | - | 清除 |
| confirmValue | - | 值一样 不触发设置值 |
| resetColor | - | 重置 |
| displayedRgb | color, showAlpha | - |

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisInput（输入框）

### 概述
ZoehisInput 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisInput from '@zoesoft.com.cn/his-component-vue/packages/input';
Vue.use(ZoehisInput);
```

### 基础用法
```vue
<zoehis-input></zoehis-input>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-input
  autosize="false"
  :validateEvent="true"
  :closeRz="true">
</zoehis-input>
```

### 常用事件使用
```vue
<template>
  <zoehis-input @input="handleEvent">
  </zoehis-input>
</template>
<script>
export default {
  methods: {
    handleEvent(val) {
      console.log('input 事件触发', val);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-input ref="myRef"></zoehis-input>
  <zoehis-button @clickenter="$refs.myRef.resizeTextarea()">调用 resizeTextarea 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| autosize | Boolean / Object | false | 自适应高度 |
| validateEvent | Boolean | true | 内部使用， 用于表单触发校验事件 |
| closeRz | Boolean | true | TODO 千万不要用，临时给小范用的，后面优化一定要删掉 |
| type | String | text | 文本类型 |
| value | - | - | 绑定值 |
| icon | String | - | 图标类名 |
| disabled | Boolean | - | 是否禁用 |
| placeholder | String | - | 占位提示 |
| width | Number / String | - | default: 115 |
| height | Number / String | - | 文本的height |
| clearable | Boolean | true | 是否启动清空功能 |
| clickselect | Boolean | true | 单击的时候文字全选 |
| textAlign | String | left | input textAlign属性 |
| maxlength | Number / String | - | input maxlength属性 |
| preventOverId | String | - | 容器 ID |
| errorTip | String | - | 错误提示 |
| checkzero | Boolean | true | 检验时要不要包括0 |
| deleteKey | Boolean | false | 要不要支持delete快捷键清空事件 |
| delayTime | String / Number | 0 | 模糊查询的延时时间 |
| showErrorTip | Boolean | false | 为true， 显示错误提示(不管是否有聚焦) |
| imeMode | String | auto | 原始属性 ime-mode （输入法引擎） |
| firstChange | Boolean | false | true代表 初始化不触发change事件，false：代表初始化触发change事件（主要历史存在的问题） |
| minErrorWidth | [String,Number] | - | 最小错误宽 |
| copyAndPasteFlag | Boolean | false | 是否开启复制粘贴功能 true：开启，false：不开启，该功能需要开启读取剪贴板权限（涉及安全性问题），慎用 默认关闭 |
| isIconStacking | Boolean | true | 内部使用，为了配合下拉框isFocusDropdown属性（聚焦不显示下拉框） ×按钮和向下箭头不叠放 |
| uiType | String | - | 配置不同的ui样式  rectangle矩形输入框 onlyBottom 用于文本域，只有底部边框 |
| textColor | - | String | 文本颜色 |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| input | text | 值变化时触发 |
| keyup | event | 键盘按键抬起时触发 |
| compositionstart | event | 输入法开始输入时触发 |
| compositionupdate | event | 输入法输入更新时触发 |
| compositionend | event | 输入法结束输入时触发 |
| change | val | 值改变时触发 |
| focus | event | 获取焦点时触发 |
| blur | event | 失去焦点时触发 |
| inputnative | value | 原生 input 事件触发 |
| clickicon | event | 点击图标时触发 |
| mousedownIcon | event | 按下图标时触发 |
| mousedownClear | event | 按下清除按钮时触发 |
| clear | event | 清空时触发 |
| click | event | 点击时触发 |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| resizeTextarea | - | 调整文本域大小 |
| setTextColor | color | 设置字体颜色 |
| resetTextColor | - | 重置字体颜色 |
| initI18n | - | 国际化初始化 |
| toggleRightMenuVisible | status | 切换状态 |
| addMousedown | - | 右键 |
| ctrlHandle | event | 不是ctrl + C 、ctrl + A、ctrl + V、ctrl + X 的组合键 阻止默认事件 |
| handleKeyup | event | 监听用户键盘输入事件 |
| handleCompositionstart | event | 内部事件处理 |
| handleCompositionUpdate | event | 内部事件处理 |
| handleCompositionEnd | event | 内部事件处理 |
| getInput | - | 获取输入框元素 |
| setPlacementToError | - | placement:位置 |
| setErrorTipWidth | - | 设置错误提示宽度 |
| doDestroy | - | 错误提示的销毁 |
| getDisabled | - | 获取是否可以编辑的状态 |
| getValue | - | 获取当前值 |
| setValue | val | 处理字数限制的 todo 这边单纯更新长度 不进行限制字数 |
| handleFocus | event | 内部事件处理 |
| handleBlur | event | 内部事件处理 |
| getBytesLength | str | 获取字符串的字节数 中文算两个字节 |
| handleInput | event | this.$emit('change', value); |
| handleClickIcon | event | 内部事件处理 |
| handleMouseDownIcon | event | 内部事件处理 |
| handleDelete | e | delete事件 |
| handleMouseDownClear | event | 内部事件处理 |
| handleClear | event | 清空前事件 |
| handleClick | event | 若选中某一部分了就不在去全选了 |
| inputSelect | - | 全选方法 |
| focus | - | 焦点聚集方法 |

### Slots（插槽）
| 插槽名 | 作用域参数 | 说明 |
|--------|-----------|------|
| prepend | - |  |
| append | - |  |

---

## zoehisInputNumber（数字输入框）

### 概述
ZoehisInputNumber 组件

### 引入方式
```js
import Vue from 'vue';
import zoehisInputNumber from '@zoesoft.com.cn/his-component-vue/packages/input-number';
Vue.use(zoehisInputNumber);
```

### 基础用法
```vue
<zoehis-input-number></zoehis-input-number>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-input-number
  step="1"
  :disabled="true">
</zoehis-input-number>
```

### 常用事件使用
```vue
<template>
  <zoehis-input-number @input="handleEvent">
  </zoehis-input-number>
</template>
<script>
export default {
  methods: {
    handleEvent(val) {
      console.log('input 事件触发', val);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-input-number ref="myRef"></zoehis-input-number>
  <zoehis-button @clickenter="$refs.myRef.focus()">调用 focus 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| step | Number | 1 | 每次增加的数值 |
| controlsPosition | String | - | 按钮位置 |
| disabled | Boolean | - | 是否禁用 |
| size | String | medium | 尺寸 |
| max | Number | Infinity | 最大值 |
| min | Number | -Infinity | 最小值 |
| originalVal | Boolean | false | 初始值给什么，就是什么，不改变值,允许value为空 |
| controls | Boolean | true | 显示按钮 |
| value | Number / String | - | 绑定值 |
| precision | Number | - | 精度 |
| width | String | - | 宽度 |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| input | newVal | 值变化时触发 |
| blur | event | 失去焦点时触发 |
| focus | event | 获取焦点时触发 |
| change | newVal, oldVal | 值改变时触发 |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| focus | - | ========================== |
| handleInputChange | e | ========================== |
| handleBlur | event | 直接改变input的值 |
| handleFocus | event | 内部事件处理 |
| getPrecision | value | 获取相关数据 |
| decrease | - | - |
| increase | - | - |
| _decrease | val, step | - |
| _increase | val, step | Solve the accuracy problem of JS decimal calculation by converting the value to integer. |
| toPrecision | num, precision | - |
| setCurrentValue | newVal | 以precision为准 |

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisManuallyInput（手动输入框）

### 概述
ZoehisManullyInput 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisManuallyInput from '@zoesoft.com.cn/his-component-vue/packages/manually-input';
Vue.use(ZoehisManuallyInput);
```

### 基础用法
```vue
<zoehis-manually-input></zoehis-manually-input>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-manually-input
  :rangeedit="true"
  :showclose="true">
</zoehis-manually-input>
```

### 常用事件使用
```vue
<template>
  <zoehis-manually-input @input="handleEvent">
  </zoehis-manually-input>
</template>
<script>
export default {
  methods: {
    handleEvent(val) {
      console.log('input 事件触发', val);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-manually-input ref="myRef"></zoehis-manually-input>
  <zoehis-button @clickenter="$refs.myRef.createdInitFunc()">调用 createdInitFunc 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| uiType | String | - | 配置不同的ui样式  默认是带下划线的输入框，rectangle矩形带边框的输入框 |
| rangeedit | Boolean | false | - |
| showclose | Boolean | false | 显示关闭 |
| disabled | Boolean | false | 是否禁用 |
| endDateOver | Boolean | false | 是否以23:59:59结尾 |
| mode | String | normal | 日期模式，normal:单日期， range:日期区间 |
| width | String / Number | 0 | 宽度 |
| height | String / Number | - | 高度 |
| readonly | Boolean | false | 是否只读 |
| disabled | Boolean | false | 是否禁用 |
| format | String | yyyy-MM-dd | 显示格式 |
| imeMode | String | disabled | 输入法 |
| value | [String, Object] | - | 绑定值 |
| placeholder | String | 请输入 | 占位提示 |
| clickselect | Boolean | true | 点击全选 |
| type | String | text | 类型 |
| hasTime | Boolean | false | 是否包含时分秒 |
| showErrorTip | Boolean | false | 为true， 显示错误提示 |
| errorTip | String | - | 错误的提示内容 为 "" 时表示关闭错误的提示 |
| clearable | Boolean | true | 可取消选中 |
| icon | String | - | 图标类名 |
| beforeClick | Function | - | 点击前回调 |
| minErrorWidth | [String,Number] | - | 最小错误宽 |
| splitText | String |  至  | 时间区间分割文字 |
| correction | Number | 1 | - |
| enabledRange | Array | - | - |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| input | {startDate: this.dateVal, endDate: this.endDateVal} | 值变化时触发 |
| clickInput | - | - |
| keydown | e | - |
| clear | - | 清空时触发 |
| enter | - | 按下回车时触发 |
| keyup | e | 键盘按键抬起时触发 |
| change | this.adjustDate(this.inputVal | 值改变时触发 |
| focus | - | 获取焦点时触发 |
| blur | this.value, this.inputVal | 失去焦点时触发 |
| skipAction | curInfo | - |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| createdInitFunc | - | 初始化宽度 |
| focus | - | 手动聚焦 |
| setPlacementToError | placement | 设置错误提示位置 |
| clickInput | e | - |
| handleKeydown | e | this.changePanel(false); |
| handleKeyup | e | 内部事件处理 |
| clickClearDate | - | - |
| setDateTime | date, endDate | 设置相关数据 |
| adjustDate | dateStr | 如果只有月日，补充年份 |
| initFunc | date, endDate, isFirst | this.$emit('input', this.inputVal); |
| doKeyUpDate | e | } |
| m_getKeyDate | keyStart, keyEnd, keyType, type='year' | 选中秒 |
| getMatchIndex | - | 如果结尾是非数字， 焦点可以选中结尾 |
| getKeySelection | keyStart, moveType=0 | 加个容错，indexArr[keyNum]可能不存在 |
| ejectValue | - | 重新选中 |
| transFormStrToDate | str | - |
| getDateJsontoStamp | dateJson | 获取相关数据 |
| keyHandleDate | keyStart, keyEnd, keyType | 重新选中 |
| checkoutWrite | keyCode | } |
| handleLRKey | { keyCode, value, indexStart, indexEnd} | 内部事件处理 |
| focusInput | - | - |
| clickIcon | - | - |
| inputBlur | - | 有时分秒的情况 |
| doDealTime | - | this.oneDateJson = this.initDate; |
| doKeyupNum | indexStart, indexEnd, val | 跳转到下一位 |
| getStartEndDate | - | } |
| selectInput | sIndex, eIndex, isMounted=false | - |
| isValidTime | str | Replace format placeholders with regex patterns |
| keyEnterHandle | type=null | 当前改变的是结束日期 |
| keyEnterHandle2 | - | 有显示年份的 |
| getDateJson | time | 获取相关数据 |
| getDayCountOfMonth | year, month | 获取相关数据 |

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisMultipleSelect（多选下拉）

### 概述
ZoehisMultipleSelect 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisMultipleSelect from '@zoesoft.com.cn/his-component-vue/packages/multiple-select';
Vue.use(ZoehisMultipleSelect);
```

### 基础用法
```vue
<zoehis-multiple-select></zoehis-multiple-select>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-multiple-select
  :firstFocQuery="true"
  :closeRz="true">
</zoehis-multiple-select>
```

### 常用事件使用
```vue
<template>
  <zoehis-multiple-select @input="handleEvent">
  </zoehis-multiple-select>
</template>
<script>
export default {
  methods: {
    handleEvent(val) {
      console.log('input 事件触发', val);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-multiple-select ref="myRef"></zoehis-multiple-select>
  <zoehis-button @clickenter="$refs.myRef.navigateNext()">调用 navigateNext 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| firstFocQuery | Boolean | false | 设置第一次聚焦焦点聚焦的位置，false：聚焦到外框上输入框上，true：聚焦到搜索框上，默认为false |
| closeRz | Boolean | true | TODO 千万不要用，临时给小范用的，后面优化一定要删掉 |
| value | - | - | 输入框 v-model的值[] |
| selectdata | Array | - | 下拉的所有数据 |
| placeholder | String | - | 默认显示的提示文字 |
| itemcode | String | id | 下拉的选项的唯一值 |
| itemtext | String | text | 下拉选项的文本值 |
| filterField | Array | - | 需要过滤的几个字段  默认为：名称、code值、拼音码、五笔码 |
| dropdownMaxWidth | Number / String | 500px | 下拉框的最大宽度 |
| width | Number / String | 165 | 下拉的宽度 |
| renderItem | Function | - | 自定义项 |
| noMatchText | String | - | 无匹配文本 |
| defaultfirst | Boolean | true | 默认首项 |
| delayTime | String / Number | 200 | 模糊查询的延时时间（只对filterMethod方法有效） |
| preventOverId | String | - | 容器 ID |
| clearable | Boolean | true | 是否支持XX清空功能 |
| imeMode | String | auto | 原始属性 ime-mode （输入法引擎） |
| delimiter | String | ; | 显示的分隔符 |
| maxHeight | [Number, String] | - | 最大高度 |
| uiType | String | - | 配置不同的ui样式  默认是带下划线的下拉框，rectangle矩形带边框的下拉框 |
| align | String | left | 弹窗对齐方式 |
| showAll | Boolean | false | 是否默认显示全部 |
| showAllBt | Boolean | false | 是否显示全选按钮 |
| isWrap | Boolean | false | 是否换行 |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| input | [] | 值变化时触发 |
| change | this.selected | 值改变时触发 |
| clear | - | 清空时触发 |
| drop-keydown | event | - |
| drop-keyup | event | - |
| focus | - | 获取焦点时触发 |
| blur | - | 失去焦点时触发 |
| scroll-to-bottom | query | 滚动到底部时触发 |
| scroll-to-top | query | 滚动到顶部时触发 |
| scroll-after | query | 滚动后触发 |
| searchClear | e | - |
| select-change | temArr,curSelect,status | - |
| visible-change | val | 显隐状态变化时触发 |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| navigateNext | - | - |
| handleBoxEnter | - | 因为弹出窗有设置 keyup.enter事件，所以只要焦点到输入框上就可 |
| navigatePrev | - | - |
| clearEvn | - | 清空事件 |
| keyupDropdownEvn | - | 下拉框 输入框事件 |
| keyupDropupEvn | - | 下拉框 输入框事件 |
| multipleFocus | - | 组件聚焦的事件 |
| multipleBlur | type | 获取具有焦点的元素 |
| hide | - | 隐藏当前下拉组件的弹窗 |
| scrollToBottomEvn | query | 滚动条滚到底事件 |
| scrollToTopEvn | query | 滚动条事件 |
| scrollAfterEvn | query | 滚动条事件 |
| searchClearEvn | e | 搜索栏清空事件事件 |
| handleFocusBox | - | 当前有选中的数据 |
| handleKeydown | e,d,index | 过滤 ctrl&&tab || ctrl&&enter || ctrl||C |
| deleteItem | d | 删除某一选项 |
| handleSelectChange | arr,curSelect,status | 值选中改变事件 |
| handleFocus | d,index | 输入框焦点定位 |
| focusInput | index | 焦点定位到输入区 |
| focus | - | 手动聚焦 |
| focusQuery | - | 焦点定位到搜索区 |
| setSelectVal | val=[] | 设置弹窗的选中值 |
| setInputWidth | - | 先用下拉框的长度 || 输入框的长度 || 默认长度 |
| isNumBool | val | 判断val 是不是数字 |
| setRenderShowAll | - | 设置全部的数据 |
| showEllipsis | - | 40 为内容距离右边的边距，超过就显示... |
| renderInput | d, index | - |

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisRadio（单选框）

### 概述
ZoehisRadio 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisRadio from '@zoesoft.com.cn/his-component-vue/packages/radio';
Vue.use(ZoehisRadio);
```

### 基础用法
```vue
<zoehis-radio></zoehis-radio>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-radio>
</zoehis-radio>
```

### 常用事件使用
```vue
<template>
  <zoehis-radio @input="handleEvent">
  </zoehis-radio>
</template>
<script>
export default {
  methods: {
    handleEvent(val) {
      console.log('input 事件触发', val);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-radio ref="myRef"></zoehis-radio>
  <zoehis-button @clickenter="$refs.myRef.iFocus()">调用 iFocus 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| tabNum | - | - | - |
| value | - | - | 绑定值 |
| label | - | - | 标签文本 |
| disabled | Boolean | - | 是否禁用 |
| name | String | - | 文件字段名 |
| tabindex | - | - | Tab 索引 |
| isShowFocus | Boolean | false | 显示焦点 |
| clearable | Boolean | false | 是否允许它点击清空，即当前已选中，再次点击单选框则取消选中 |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| input | val | 值变化时触发 |
| inputnative | val | 原生 input 事件触发 |
| click | this.model, this.label | 点击时触发 |
| focus | this.label | 获取焦点时触发 |
| blur | this.label | 失去焦点时触发 |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| iFocus | e | - |
| focus | - | 手动聚焦 |
| handleClick | e | 清空当前选中 |
| goOn | - | - |
| handleFocus | - | 内部事件处理 |
| handleBlur | - | 内部事件处理 |

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisSelect（下拉选择）

### 概述
ZoehisSelect 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisSelect from '@zoesoft.com.cn/his-component-vue/packages/select';
Vue.use(ZoehisSelect);
```

### 基础用法
```vue
<zoehis-select></zoehis-select>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-select
  :native="true"
  :isFocusDropdown="true"
  :closeRz="true">
</zoehis-select>
```

### 常用事件使用
```vue
<template>
  <zoehis-select @refresh-bt-click="handleEvent">
  </zoehis-select>
</template>
<script>
export default {
  methods: {
    handleEvent(event) {
      console.log('refresh-bt-click 事件触发', event);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-select ref="myRef"></zoehis-select>
  <zoehis-button @clickenter="$refs.myRef.setTextColor()">调用 setTextColor 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| native | Boolean | true | 原生滚动 |
| isFocusDropdown | Boolean | true | - |
| closeRz | Boolean | true | TODO 千万不要用，临时给小范用的，后面优化一定要删掉 |
| clearable | Boolean | true | 是否支持XX清空功能 |
| placeholder | String | - | 默认显示的提示文字 |
| value | - | - | 绑定值 |
| textvalue | [Number, String] | - | 文本值 |
| delayTime | String / Number | 200 | 模糊查询的延时时间（只对filterMethod方法有效） |
| noMatchText | String | - | 无匹配文本 |
| renderShowData | Function | - | 自定义显示 |
| renderItem | Function | - | 自定义项 |
| selectdata | Array | - | 下拉的所有数据 |
| itemcode | String | id | 下拉的选项的唯一值 |
| itemtext | String | text | 下拉选项的文本值 |
| itemDisabled | String | disabled | 下拉选项的文本值 |
| width | Number / String | 115 | 下拉的宽度 |
| dropdownMaxWidth | Number / String | 800 | 下拉框的最大宽度 不包含明细弹窗 |
| noClearBlur | Boolean | false | 焦点离开是否不清空数据，默认false代表情况数据 |
| defaultfirst | Boolean | true | 默认首项 |
| preventOverId | String | - | 容器 ID |
| filterField | Array | - | 需要过滤的几个字段  默认为：名称、code值、拼音码、五笔码 |
| errorTip | String | - | 错误的提示内容 为 "" 时表示关闭错误的提示 |
| showErrorTip | Boolean | false | 为true， 显示错误提示 |
| imeMode | String | disabled | 原始属性 ime-mode （输入法引擎） |
| align | String | left | 设置弹出窗的位置（左对齐、居中对齐、右对齐） left center right |
| minErrorWidth | [String,Number] | - | 最小错误宽 |
| uiType | String | - | 配置不同的ui样式  默认是带下划线的下拉框，rectangle矩形带边框的下拉框, simple是无边框的下拉框 |
| iconFont | String | - | 自定义下拉图标样式 |
| validateEvent | Boolean | true | 内部使用 |
| clickselect | Boolean | true | 单击的时候文字全选,下拉框这边主要是通过触发事件去设置选中 |
| textColor | String | - | 文本颜色 |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| refresh-bt-click | - | 点击刷新按钮时触发 |
| update:textvalue | this.selectedLabel | - |
| blur | - | 失去焦点时触发 |
| scroll-after | this.query, this | 滚动后触发 |
| scroll-to-bottom | this.query, this | 滚动到底部时触发 |
| scroll-to-top | this.query, this | 滚动到顶部时触发 |
| input | '' | 值变化时触发 |
| focus | - | 获取焦点时触发 |
| clear | - | 清空时触发 |
| clickicon | e | 点击图标时触发 |
| enter | - | 按下回车时触发 |
| selected | option.value, option.item | 选中时触发 |
| change | val, null | 值改变时触发 |
| visible-change | val, this | 显隐状态变化时触发 |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| setTextColor | color | 设置字体颜色 |
| resetTextColor | - | 重置字体颜色 |
| handleRefreshBtClick | - | 刷新按钮点击事件 |
| placementChangeDrop | placement | 弹窗的上下位置变了 |
| handlePlacementChange | - | bottom、top互换 |
| focusDrop | - | - |
| mousedownDrop | - | 不用focusin的事件是因为 鼠标点击不放时，焦点不会在弹窗上 |
| focusoutDrop | - | 弹窗焦点失去 |
| handleBlur | - | 检查浏览器是否失去焦点 |
| getFocusFlag | - | return true 代表焦点在组件上  false代表焦点不在组件上 |
| hide | - | 供外部调用隐藏方法 |
| handleKeyup | e | 此处加了延时是因为 备注："351处"  query赋值时，selectedLabel此时还没有更新到最新的 造成query的时还是为""空。 |
| getDisabled | - | 获取是否可以编辑的状态 |
| scrollMove | num | 移动滚动条 |
| getText | - | 获取文本 |
| getSelected | - | uiInitFlag 说明需要在selectdata上重新匹配一下 |
| doDestroy | - | 遮罩对象销毁 |
| focus | index | 焦点聚集的方法 |
| scrollafter | wrap | 滚动条滚快顶了的事件 |
| initSelectData | - | 初始化选中项 |
| initSelData | - | 初始化 |
| getRowData | - | 获取行数据 |
| setSelectedLabel | val | 设置要显示的值 |
| onOptionDestroy | option | 销毁选项 |
| onInputChange | val | 每次查询，滚动的重置到最顶 |
| onCompositionEnd | e | 修复在中文输入法直接点击结果不查询 |
| scrollToOption | p | 没有选中项则定位到0 |
| handleClose | - | 点击其他地方关闭弹窗 |
| handleFocus | - | 清一下定时器 防止焦点是从弹窗那边 然后再点击输入框的情况 |
| handleMouseDown | event | 判断当前焦点的元素是否是select组件里的其中一个input元素 |
| handleMousedownClear | - | 按着不放清空事件 |
| handleClear | event | 清空事件 |
| handleClearData | isClickClear = true | 第二个参数代表的是弹窗显示的时候触发的事件 用于过滤前端显示的数据 |
| toggleMenu | e | 是可以点击的下拉框 |
| handleTab | e | 快捷键tab事件 |
| navigateOptions | direction | 若是不可以点击的、组的、隐藏的，则进行下一个定位 |
| getOption | value | 若是虚拟视窗的，且在 cachedOptions 里找不到的，则去 selectdata里找 |
| setSelected | - | 解决设置v-model值 没有找到对应的没有把文本清楚掉 |
| selectOption | - | 修复下拉没有展开的时候一直去按enter造成的bug |
| deleteSelected | - | 删除选中的操作，并隐藏下拉 |
| resetHoverIndex | isQuery | 找不到选中的行 同时需要默认值 |
| beforeOptionSelect | option, isEnter | beforeSelect返回false 表示阻止选中 否则选中 |
| handleOptionSelect | option, isEnter | 解决点击完选项之后 焦点没有在输入框上的bug |
| isNoSelectData | val | 判断选中的值是否是空 |
| setInputWidth | - | 设置错误提示 |
| resetSelectPos | val | 滚动到选中的行 |
| isWidthNumber | val | 判断是否是数字 |

### Slots（插槽）
| 插槽名 | 作用域参数 | 说明 |
|--------|-----------|------|
| customItem | - |  |

---

## ZoehisSlider（滑块）

### 概述
ZoehisSlider 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisSlider from '@zoesoft.com.cn/his-component-vue/packages/slider';
Vue.use(ZoehisSlider);
```

### 基础用法
```vue
<zoehis-slider></zoehis-slider>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-slider
  min="0"
  max="100"
  step="1">
</zoehis-slider>
```

### 常用事件使用
```vue
<template>
  <zoehis-slider @input="handleEvent">
  </zoehis-slider>
</template>
<script>
export default {
  methods: {
    handleEvent(val) {
      console.log('input 事件触发', val);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-slider ref="myRef"></zoehis-slider>
  <zoehis-button @clickenter="$refs.myRef.valueChanged()">调用 valueChanged 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| min | Number | 0 | 最小值 |
| max | Number | 100 | 最大值 |
| step | Number | 1 | 步长 |
| value | Number / Array | 0 | 绑定值 |
| showInput | Boolean | false | 是否显示输入框 |
| showInputControls | Boolean | true | - |
| inputSize | String | small | 输入框尺寸 |
| showStops | Boolean | false | 是否显示间断点 |
| showTooltip | Boolean | true | 是否显示提示 |
| formatTooltip | Function | - | 格式化提示 |
| disabled | Boolean | false | 是否禁用 |
| range | Boolean | false | 是否范围选择 |
| vertical | Boolean | false | 是否垂直 |
| height | String | - | 高度 |
| debounce | Number | 300 | 防抖延迟 |
| label | String | - | 标签文本 |
| tooltipClass | String | - | - |
| uiType | String | - | UI 类型 |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| input | [this.minValue, this.maxValue] | 值变化时触发 |
| change | this.range ? [this.minValue, this.maxValue] : this.value | 值改变时触发 |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| valueChanged | - | - |
| setValues | - | 单值处理 val值必须为 number类型 |
| setPosition | percent | 设置相关数据 |
| onSliderClick | event | - |
| resetSize | - | 重置 |
| emitChange | - | - |

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisSwitch（开关）

### 概述
ZoehisSwitch 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisSwitch from '@zoesoft.com.cn/his-component-vue/packages/switch';
Vue.use(ZoehisSwitch);
```

### 基础用法
```vue
<zoehis-switch></zoehis-switch>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-switch
  value="false"
  :disabled="true"
  width="0">
</zoehis-switch>
```

### 常用事件使用
```vue
<template>
  <zoehis-switch @input="handleEvent">
  </zoehis-switch>
</template>
<script>
export default {
  methods: {
    handleEvent(val) {
      console.log('input 事件触发', val);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-switch ref="myRef"></zoehis-switch>
  <zoehis-button @clickenter="$refs.myRef.setBackgroundColor()">调用 setBackgroundColor 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| value | Boolean / String / Number | false | 绑定值 |
| disabled | Boolean | false | 是否禁用 |
| width | Number | 0 | 宽度 |
| ontext | String | - | - |
| offtext | String | - | - |
| oncolor | String | - | - |
| offcolor | String | - | - |
| onvalue | Boolean / String / Number | true | - |
| offvalue | Boolean / String / Number | false | - |
| name | String | - | 文件字段名 |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| input | this.offvalue | 值变化时触发 |
| change | this.checked ? this.offvalue : this.onvalue | 值改变时触发 |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| handleChange | - | in case parent refuses to change component's value |
| setBackgroundColor | - | 设置相关数据 |

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisUpload（上传）

### 概述
ZoehisUpload 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisUpload from '@zoesoft.com.cn/his-component-vue/packages/upload';
Vue.use(ZoehisUpload);
```

### 基础用法
```vue
<zoehis-upload></zoehis-upload>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-upload
  :withcredentials="true">
</zoehis-upload>
```

### 常用事件使用
```vue
<template>
  <zoehis-upload @previewClose="handleEvent">
  </zoehis-upload>
</template>
<script>
export default {
  methods: {
    handleEvent(val) {
      console.log('previewClose 事件触发', val);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-upload ref="myRef"></zoehis-upload>
  <zoehis-button @clickenter="$refs.myRef.addFile()">调用 addFile 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| action | String | - | 必选参数，上传的地址 |
| headers | Object | - | 设置上传的请求头部 |
| withcredentials | Boolean | true | 支持发送 cookie 凭证信息 |
| name | String | file | 上传的文件字段名 |
| autoupload | Boolean | true | 是否在选取文件后立即进行上传 |
| onprogress | Function | noop | 文件上传时的钩子 |
| onsuccess | Function | noop | 文件上传成功时的钩子 |
| onerror | Function | noop | 文件上传失败时的钩子 |
| progressEn | Function | noop | 文件上传时的钩子 |
| successEn | Function | noop | 文件上传成功时的钩子 |
| errorEn | Function | noop | 文件上传失败时的钩子 |
| showtip | Boolean | true | 显示提示 |
| disabled | Boolean | false | 是否禁用 |
| multiple | Boolean | false | 是否支持多选文件 |
| drag | Boolean | false | 是否启用拖拽上传 |
| showFileList | Boolean | false | 是否显示已上传文件列表 |
| removeEn | Function | noop | 移除回调 |
| changeEn | Function | noop | - |
| fileList | Array | - | }, // 点击文件列表中已上传的文件时的钩子 |
| uiType | String | text' // text | UI 类型 |
| exceedEn | Function | noop | 文件超出个数限制时的钩子 |
| showPreview | Boolean | true | - |
| previewCustom | Object | - | 预览弹窗自定义属性 width height title |
| addButtonCustom | Object | - | 添加按钮自定义属性 width height |
| thumbnailCustom | Object | - | 缩略图自定义属性 width height |
| size | String | big | uiType为picture-card生效，big / normal |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| previewClose | previewFile | - |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| addFile | file | 新增文件 |
| delFile | uid | 删除文件 |
| getUploadFiles | - | 获取最新的文件列表 |
| handleStart | rawFile | 输出base64 |
| handleProgress | ev, rawFile | 内部事件处理 |
| handleSuccess | res, rawFile, res2 | } |
| handleError | errMessage, rawFile, err | fileList.splice(fileList.indexOf(file), 1); |
| handleRemove | file, raw | 内部事件处理 |
| getFile | rawFile | 获取相关数据 |
| abort | file | - |
| clearFiles | - | 清除 |
| submit | file = {} | - |
| getPreviewVisible | previewVisible | 获取相关数据 |
| getPreviewFile | file | 获取相关数据 |
| closePreviewVisible | previewFile | - |
| changeFileStatus | uid, status, percentage | TODO 因为图片预览时上传成功后不删除列表文件，所以需要循环转换文件状态 |

### Slots（插槽）
该组件未定义插槽。

---

