# 导航组件

## 目录

- [ZoehisDropdown](#zoehisdropdown)
- [ZoehisMenu](#zoehismenu)
- [ZoehisMenuGroup](#zoehismenugroup)
- [ZoehisNavMenu](#zoehisnavmenu)
- [ZoehisPage](#zoehispage)
- [ZoehisTab](#zoehistab)
- [ZoehisVerticalMenu](#zoehisverticalmenu)

---

## ZoehisDropdown

### 概述
ZoehisDropdown 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisDropdown from '@zoesoft.com.cn/his-component-vue/packages/dropdown';
Vue.use(ZoehisDropdown);
```

### 基础用法
```vue
<zoehis-dropdown></zoehis-dropdown>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-dropdown
  trigger="hover"
  menuAlign="right">
</zoehis-dropdown>
```

### 常用事件使用
```vue
<template>
  <zoehis-dropdown @visible-change="handleEvent">
  </zoehis-dropdown>
</template>
<script>
export default {
  methods: {
    handleEvent(val) {
      console.log('visible-change 事件触发', val);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-dropdown ref="myRef"></zoehis-dropdown>
  <zoehis-button @clickenter="$refs.myRef.show()">调用 show 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| trigger | String | hover |  |
| menuAlign | String | right |  |
| size | String | - |  |
| splitButton | Boolean | - |  |
| hideOnClick | Boolean | true |  |
| disabled | Boolean | false |  |
| uiType | String | - | 下拉菜单UI类型 rectangle矩形带边框的下拉菜单 |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| visible-change | val |  |
| command | command, instance |  |
| click | - |  |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| show | - |  |
| hide | - |  |
| handleClick | - |  |
| initEvent | - |  |
| handleMenuItemClick | command, instance |  |

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisMenu

### 概述
ZoehisMenu 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisMenu from '@zoesoft.com.cn/his-component-vue/packages/menu';
Vue.use(ZoehisMenu);
```

### 基础用法
```vue
<zoehis-menu></zoehis-menu>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-menu
  :closeable="true">
</zoehis-menu>
```

### 常用事件使用
```vue
<template>
  <zoehis-menu @menuClick="handleEvent">
  </zoehis-menu>
</template>
<script>
export default {
  methods: {
    handleEvent(val) {
      console.log('menuClick 事件触发', val);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-menu ref="myRef"></zoehis-menu>
  <zoehis-button @clickenter="$refs.myRef.doContinue()">调用 doContinue 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| closeable | Boolean | false | 是否要显示关闭图标 |
| iconClass | String | - | 关闭图标类名 |
| uiType | String | - |  |
| label | - | - | 新代码 end |
| text | - | - |  |
| disabled | Boolean | false |  |
| badgeFlag | Boolean | - |  |
| badgeType | String | - |  |
| badgeValue | String / Number | - |  |
| badgeMax | String / Number | - |  |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| menuClick | this.label,oldLabel |  |
| closeIconClick | this.label |  |
| rightClickEn | type, this.label |  |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| handleShowState | boolean | 控制当前菜单是否显示 |
| handleClick | type=0 |  |
| doContinue | type |  |
| closeIconClick | - | 新代码 satrt |
| handleMenuclick | type=0 |  |
| newRightClick | e | y:鼠标当前元素偏移量y - 最外层的高度 + 当前元素的宽度的一半 - 2 |
| newRightClick | e | 鼠标当前元素偏移量x + 菜单项的内边距 - 2 |
| close | - |  |
| contMenuClick | type |  |

### Slots（插槽）
| 插槽名 | 作用域参数 | 说明 |
|--------|-----------|------|
| default | - |  |

---

## ZoehisMenuGroup

### 概述
菜单栏单项菜单

### 引入方式
```js
import Vue from 'vue';
import ZoehisMenuGroup from '@zoesoft.com.cn/his-component-vue/packages/menu-group';
Vue.use(ZoehisMenuGroup);
```

### 基础用法
```vue
<zoehis-menu-group></zoehis-menu-group>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-menu-group
  uiType="default">
</zoehis-menu-group>
```

### 常用方法使用
```vue
<template>
  <zoehis-menu-group ref="myRef"></zoehis-menu-group>
  <zoehis-button @clickenter="$refs.myRef.init()">调用 init 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| uiType | String | default | 类型 default：his旧组件 onelink：新组件 |

### ② 事件说明（Events）
该组件未触发自定义事件。

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| init | - |  |

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisNavMenu

### 概述
ZoehisNavMenu 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisNavMenu from '@zoesoft.com.cn/his-component-vue/packages/nav-menu';
Vue.use(ZoehisNavMenu);
```

### 基础用法
```vue
<zoehis-nav-menu></zoehis-nav-menu>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-nav-menu
  :collapse="true"
  :collapseTransition="true">
</zoehis-nav-menu>
```

### 常用事件使用
```vue
<template>
  <zoehis-nav-menu @collapseChange="handleEvent">
  </zoehis-nav-menu>
</template>
<script>
export default {
  methods: {
    handleEvent(val) {
      console.log('collapseChange 事件触发', val);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-nav-menu ref="myRef"></zoehis-nav-menu>
  <zoehis-button @clickenter="$refs.myRef.changePopVisible()">调用 changePopVisible 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| defaultActive | String | - | 当前激活菜单的 index |
| collapse | Boolean | false | 是否水平折叠收起菜单（仅在 mode 为 vertical 时可用） |
| collapseTransition | Boolean | true | 是否开启折叠动画 |
| collapsible | Boolean | true | 是否启动菜单收缩功能 |
| multicolor | Boolean | false | 是否启用双色(svg)图标 |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| collapseChange | this.collapseFlag |  |
| change | val, title |  |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| changePopVisible | val |  |
| toggleEvn | - | 折叠收起菜单 |
| select | val, keyPath | 菜单选中激活 |
| change | val, title |  |
| getParentTag | startTag,arr | 父级标签是否是body,是着停止返回集合,反之继续 |
| open | val |  |
| close | val |  |

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisPage

### 概述
ZoehisPage 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisPage from '@zoesoft.com.cn/his-component-vue/packages/page';
Vue.use(ZoehisPage);
```

### 基础用法
```vue
<zoehis-page></zoehis-page>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-page
  :zoomable="true"
  current_page="1">
</zoehis-page>
```

### 常用事件使用
```vue
<template>
  <zoehis-page @update:current_page="handleEvent">
  </zoehis-page>
</template>
<script>
export default {
  methods: {
    handleEvent(val) {
      console.log('update:current_page 事件触发', val);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-page ref="myRef"></zoehis-page>
  <zoehis-button @clickenter="$refs.myRef.setStripFunc()">调用 setStripFunc 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| stripArr | Array | - | 设置可选条目数组 |
| zoomable | Boolean | false | 表格是否支持缩放 |
| current_page | Number | 1 | 当前页----对应data中currentPage |
| set_strip | Number | 10 | 当前显示条数----对应data中setStrip |
| isSetStrip | Boolean | true |  |
| unitText | String | - | 单位文字， |
| iconflag | Boolean | false |  |
| showtotal | Boolean | true | 是否显示总数 |
| nonetotal | Boolean | false |  |
| prevdisabled | Boolean | false | 上一页是否禁止点击（nonetotal=true有效） |
| nextdisabled | Boolean | false | 下一页是否禁止点击（nonetotal=true有效） |
| keys | Object | - |  |
| preventOverId | String | - |  |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| update:current_page | submitPage |  |
| changePage | submitPage |  |
| update:set_strip | setStrip |  |
| changeStrip | setStrip |  |
| prev-click | - |  |
| next-click | - |  |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| setStripFunc | e |  |
| showDropList | e |  |
| prevPage | - | 上一页 |
| nextPage | - | 下一页 |
| toHome | - | 首页 |
| toEnd | - | 末页 |
| before | newPage, oldPage, type = 0 | inputBlur回车跳转调用该方法会传入type=1 将页码设置回输入之前 |
| goOn | newPage | 执行切换页  newPage要跳转页码 |
| inputBlur | - | 判断输入是否是数字 |
| enterChangePage | e | 页码输入框回车事件 |

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisTab

### 概述
ZoehisTab 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisTab from '@zoesoft.com.cn/his-component-vue/packages/tab';
Vue.use(ZoehisTab);
```

### 基础用法
```vue
<zoehis-tab></zoehis-tab>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-tab
  itemcode="configId"
  itemtext="menuName">
</zoehis-tab>
```

### 常用事件使用
```vue
<template>
  <zoehis-tab @context-click="handleEvent">
  </zoehis-tab>
</template>
<script>
export default {
  methods: {
    handleEvent(val) {
      console.log('context-click 事件触发', val);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-tab ref="myRef"></zoehis-tab>
  <zoehis-button @clickenter="$refs.myRef.getItemById()">调用 getItemById 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| itemcode | String | configId |  |
| itemtext | String | menuName |  |
| currentId | String | - |  |
| tabList | Array | - |  |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| context-click | Object.assign({}, this.contextCurItem |  |
| tab-click | item, this.getItemById( |  |
| tab-close | item |  |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| getItemById | - |  |
| handleCxtClick | d |  |
| handleContext | item, e |  |
| handleBlur | - |  |
| hideContext | - |  |
| handleTabClick | item |  |
| handleClose | item |  |

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisVerticalMenu

### 概述
ZoehisVerticalMenu 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisVerticalMenu from '@zoesoft.com.cn/his-component-vue/packages/vertical-menu';
Vue.use(ZoehisVerticalMenu);
```

### 基础用法
```vue
<zoehis-vertical-menu></zoehis-vertical-menu>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-vertical-menu
  :noShrink="true">
</zoehis-vertical-menu>
```

### 常用事件使用
```vue
<template>
  <zoehis-vertical-menu @click="handleEvent">
  </zoehis-vertical-menu>
</template>
<script>
export default {
  methods: {
    handleEvent(val) {
      console.log('click 事件触发', val);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-vertical-menu ref="myRef"></zoehis-vertical-menu>
  <zoehis-button @clickenter="$refs.myRef.changeHoverId()">调用 changeHoverId 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| value | Object | - | V-model 值 |
| data | Array | - | 菜单所有数据 |
| noShrink | Boolean | true | 收缩菜单栏 |
| beforeClick | Function | - | 点击事件之前操作，方法返回false为不可选中当前菜单 |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| click | data,configId |  |
| change | data |  |
| input | data |  |
| fix-change | this.menuFixFlag |  |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| changeHoverId | d={},i=-1 | 改变hoverId的值 |
| handleFirstMenuClick | configId | 点击一级菜单 |
| handleSecondMenuClick | configId,noClick | 点击二级菜单 |
| getFixflag | - | 获取菜单栏是否伸缩 |
| setFixflag | flag | 设置菜单栏伸缩 |
| toggleclick | - | 伸缩按钮点击事件 |

### Slots（插槽）
该组件未定义插槽。

---

