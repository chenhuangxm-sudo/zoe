# 反馈组件

## 目录

- [ZoehisDialog（弹窗）](#zoehisdialog)
- [ZoehisDrawer（抽屉）](#zoehisdrawer)
- [ZoehisLoading（加载）](#zoehisloading)
- [ZoehisMessageBox（消息弹窗）](#zoehismessagebox)
- [ZoehisPopover（弹出框）](#zoehispopover)
- [zoeMessage（消息提示）](#zoemessage)

---

## ZoehisDialog（弹窗）

### 概述
ZoehisDialog 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisDialog from '@zoesoft.com.cn/his-component-vue/packages/dialog';
Vue.use(ZoehisDialog);
```

### 基础用法
```vue
<zoehis-dialog></zoehis-dialog>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-dialog
  :native="true"
  :modal="true">
</zoehis-dialog>
```

### 常用事件使用
```vue
<template>
  <zoehis-dialog @headerclose="handleEvent">
  </zoehis-dialog>
</template>
<script>
export default {
  methods: {
    handleEvent(event) {
      console.log('headerclose 事件触发', event);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-dialog ref="myRef"></zoehis-dialog>
  <zoehis-button @clickenter="$refs.myRef.scrollMove()">调用 scrollMove 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| native | Boolean | false | 原生滚动 |
| title | String | - | 弹窗标题 为""空的话，不显示弹窗标题 |
| modal | Boolean | true | 是否显示黑色遮罩 |
| modalAppendToBody | Boolean | false | 黑色遮罩的位置//body下或者当前弹窗的同级下 |
| width | Number / String | 420 | 宽度 |
| height | Number / String | 235 | 高度 |
| closeOnClickModal | Boolean | false | 点击遮罩是否关闭弹窗、false：不关闭 |
| closeOnPressEscape | Boolean | true | 按快捷键esc是否关闭弹窗、false：不关闭；true关闭 |
| errorSlideDownName | String | - | 收起详情文本 |
| errorSlideUpName | String | - | 展开详情文本 |
| headerclose | Boolean | - | 显示关闭按钮 |
| supportdrag | Boolean | true | 是否支持拖拉 |
| newscrollbar | Boolean | false | 是否是最终的滚动条优化 |
| bodyscroll | Boolean | false | 内容部分是否需要滚动条 |
| appendToBody | Boolean | false | 是否将弹窗插入到body下 |
| positionStyle | Object | - | 初始化时的位置 { top, left } |
| alwaysInitPosition | Boolean | false | 每次打开弹窗都初始化弹窗位置 |
| mode | String | - | 模式，mode='new'时，为新版弹窗，支持高度自适应 |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| headerclose | - | 点击关闭按钮时触发 |
| update:visible | false | 显隐状态更新时触发 |
| open | - | 打开时触发 |
| close | - | 关闭时触发 |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| scrollMove | left=0,top=0 | 滚动条的位置 |
| handleHeaderClose | - | 内部事件处理 |
| computedStyleObj | obj = {} | - |
| initStyle | - | 初始化 |
| alwaysInitStyle | - | - |
| setVmPosition | { left, top, right, bottom } | 设置相关数据 |
| errorToggleClick | - | - |
| toggleErrorDetail | val | this.eHeight = 0 |
| handleWrapperClick | - | 缩放结束的触发不算 |
| handleClose | flag="" | 关闭前事件 若父组件有定义beforeClose变量 要关闭时直接执行（参数的方法） |
| hide | cancel | 隐藏 |
| show | - | 显示弹窗的方法 |
| handleMousedown | type,ev | 按下时创建一个事件 |
| winDispatchEvent | name='zoehisDialogOpened' | name: 触发名称 |
| getNewHeight | - | 获取最新高度 |

### Slots（插槽）
| 插槽名 | 作用域参数 | 说明 |
|--------|-----------|------|
| footer | - |  |
| error | - |  |

---

## ZoehisDrawer（抽屉）

### 概述
ZoehisDrawer 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisDrawer from '@zoesoft.com.cn/his-component-vue/packages/drawer';
Vue.use(ZoehisDrawer);
```

### 基础用法
```vue
<zoehis-drawer></zoehis-drawer>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-drawer
  :appendToBody="true">
</zoehis-drawer>
```

### 常用事件使用
```vue
<template>
  <zoehis-drawer @open="handleEvent">
  </zoehis-drawer>
</template>
<script>
export default {
  methods: {
    handleEvent(event) {
      console.log('open 事件触发', event);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-drawer ref="myRef"></zoehis-drawer>
  <zoehis-button @clickenter="$refs.myRef.afterEnter()">调用 afterEnter 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| appendToBody | Boolean | true | 插入 body |
| beforeClose | Function | - | 关闭前回调 |
| customClass | String | - | 自定义类名 |
| closeOnPressEscape | Boolean | false | ESC 关闭 |
| destroyOnClose | Boolean | false | 关闭销毁 |
| direction | String | rtl | 方向 |
| modalAppendToBody | Boolean | true | 遮罩插入 body |
| showClose | Boolean | true | 显示关闭 |
| size | String | 260px | 尺寸 |
| title | String | - | 标题 |
| visible | Boolean | - | 是否可见 |
| wrapperClosable | Boolean | false | 点击遮罩关闭 |
| withHeader | Boolean | true | 显示头部 |
| modal | Boolean | true | 是否显示遮罩 |
| mask | Boolean | true | 是否要挡住后面的内容 |
| wraperStl | Object | - | 覆盖 wraper 的样式 |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| open | - | 打开时触发 |
| close | - | 关闭时触发 |
| opened | - | 打开动画结束时触发 |
| closed | - | 关闭动画结束时触发 |
| update:visible | false | 显隐状态更新时触发 |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| afterEnter | - | - |
| afterLeave | - | - |
| hide | cancel | 隐藏 |
| handleWrapperClick | - | 不挡住后面的 则不关闭弹窗 |
| closeDrawer | - | - |
| handleClose | - | This method also calls `beforeClose` if there was one. |

### Slots（插槽）
| 插槽名 | 作用域参数 | 说明 |
|--------|-----------|------|
| title | - |  |

---

## ZoehisLoading（加载）

### 概述
ZoehisLoading 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisLoading from '@zoesoft.com.cn/his-component-vue/packages/loading';
Vue.use(ZoehisLoading);
```

### 基础用法
```vue
<zoehis-loading></zoehis-loading>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-loading>
</zoehis-loading>
```

### 常用事件使用
```vue
<template>
  <zoehis-loading @afterleave="handleEvent">
  </zoehis-loading>
</template>
<script>
export default {
  methods: {
    handleEvent(event) {
      console.log('afterleave 事件触发', event);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-loading ref="myRef"></zoehis-loading>
  <zoehis-button @clickenter="$refs.myRef.setText()">调用 setText 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| loadType | String | - | circle圆环进度条 默认是圆点圆圈进度条 |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| afterleave | - | - |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| handleAfterLeave | - | 内部事件处理 |
| setText | text | 文字描述 |
| setType | type | 设置相关数据 |
| setState | val | 加载状态 loading记载中  error加载失败 |
| setDescribe | val | 刷新结果详细信息 |
| setRefresh | val | 是否显示刷新按钮 |
| refreshBtn | - | - |
| setVisibleTrue | val | 设置相关数据 |
| setTheme | val | 设置主题 white白色默认 black黑色 |

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisMessageBox（消息弹窗）

### 概述
MessageBox 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisMessageBox from '@zoesoft.com.cn/his-component-vue/packages/message-box';
Vue.use(ZoehisMessageBox);
```

### 基础用法
```vue
<zoehis-message-box></zoehis-message-box>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-message-box
  modal="true"
  closeOnClickModal="false">
</zoehis-message-box>
```

### 常用方法使用
```vue
<template>
  <zoehis-message-box ref="myRef"></zoehis-message-box>
  <zoehis-button @clickenter="$refs.myRef.focusBtn()">调用 focusBtn 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| size | String | - | 弹窗尺寸 small: 小尺寸弹窗，large：大尺寸弹窗，常用于错误提示 |
| modal | - | true | 显示遮罩 |
| closeOnClickModal | - | false | 点击遮罩是否关闭弹窗、false：不关闭 |
| closeOnPressEscape | - | true | 按快捷键esc是否关闭弹窗、false：不关闭；true关闭 |
| iconType | String | - | 配置提示信息图标，选择的值success error question warn |

### ② 事件说明（Events）
该组件未触发自定义事件。

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| focusBtn | type | 不聚焦--取消焦点 |
| contentClickEn | - | - |
| sendManagerEn | - | 发送给管理员事件 |
| getErrorSupplement | - | 获取相关数据 |
| copyEn | - | 一键复制 |
| setSendManagerFlag | isTrue=false | 设置发送管理员 的名称 |
| winDispatchEvent | name='zoehisDialogOpened' | name: 触发名称 |
| handleKeydown | index,e | 按钮快捷键 |
| getNextFocus | arr,index | 获取下一个焦点的对象 |
| getPrevFocus | arr,index | 获取上一个焦点的对象 |
| errorToggleClick | - | dom元素显示出来才执行initStyle |
| solutionToggleClick | - | dom元素显示出来才执行initStyle |
| initStyle | fixedLeft | 若有自定义top值，则使用自定义top值 |
| handleMousedown | ev | y=鼠标相对于网页的y左边-网页被卷去的高-待移动对象的左上边距 |
| handleWrapperClick | - | 内部事件处理 |
| handleAction | action | 内部事件处理 |
| doClose | - | 关闭弹窗的回调函数 |
| hide | - | 供外部调用 |

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisPopover（弹出框）

### 概述
ZoehisPopover 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisPopover from '@zoesoft.com.cn/his-component-vue/packages/popover';
Vue.use(ZoehisPopover);
```

### 基础用法
```vue
<zoehis-popover></zoehis-popover>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-popover
  :visibleFlag="true">
</zoehis-popover>
```

### 常用事件使用
```vue
<template>
  <zoehis-popover @show="handleEvent">
  </zoehis-popover>
</template>
<script>
export default {
  methods: {
    handleEvent(event) {
      console.log('show 事件触发', event);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-popover ref="myRef"></zoehis-popover>
  <zoehis-button @clickenter="$refs.myRef.focusoutEvn()">调用 focusoutEvn 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| visibleFlag | Boolean | false | 可见标识 |
| width | Number / String | - | 宽度 |
| height | Number / String | - | 高度 |
| trigger | String | hover | 触发方式hover/click/manual |
| placement | String | right | 弹出位置right/left/bottom/top |
| offsetX | Number / String | - | 弹出位置水平方向的偏移量 |
| offsetY | Number / String | - | 弹出位置垂直方向的偏移量 |
| disabled | Boolean | false | 是否禁用 |
| nestedFlag | Boolean | false | 是否是嵌套模式，如果是，则不能根据鼠标移入移出来显示隐藏popover |
| hidePop | Boolean | false | 是否隐藏popover,在nestedFlag为true的情况下生效 |
| preventOverId | String | - | 用于设置下拉的容器的 ID的名称 必须是唯一的 |
| appendToBody | Boolean | true | 插入 body |
| isNesting | Boolean | false | 是否嵌套使用 级联组件内部使用 |
| isShowPattern | Boolean | false | 内部使用，是否使用v-show |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| show | - | - |
| hide | - | - |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| focusoutEvn | e | - |
| mouseenterEvn | - | - |
| mouseleaveEvn | - | - |
| clickEvt | flag | - |
| show | - | 显示 |
| hide | - | 隐藏 |
| clearHideTimer | - | 清除 |
| popupLeaveEn | - | - |
| handleDocumentClick | e | 内部事件处理 |

### Slots（插槽）
| 插槽名 | 作用域参数 | 说明 |
|--------|-----------|------|
| reference | - |  |

---

## zoeMessage（消息提示）

### 概述
zoeMessage 组件

### 引入方式
```js
import Vue from 'vue';
import zoeMessage from '@zoesoft.com.cn/his-component-vue/packages/zoeMessage';
Vue.use(zoeMessage);
```

### 基础用法
```vue
<zoe-message></zoe-message>
```

### 常用方法使用
```vue
<template>
  <zoe-message ref="myRef"></zoe-message>
  <zoehis-button @clickenter="$refs.myRef.clearTimer()">调用 clearTimer 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
该组件未定义 Props。

### ② 事件说明（Events）
该组件未触发自定义事件。

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| clearTimer | - | 清除 |
| startTimer | - | - |
| close | - | 关闭 |
| handleAfterLeave | - | 销毁 |

### Slots（插槽）
该组件未定义插槽。

---

