# 日期选择组件

## 目录

- [ZoehisDateBtn（日期选择）](#zoehisdatebtn)
- [ZoehisDateEditor（日期编辑）](#zoehisdateeditor)
- [ZoehisMonthPanel（月份选择）](#zoehismonthpanel)
- [ZoehisNewDatePanel（新版日期选择）](#zoehisnewdatepanel)
- [ZoehisQuarterPicker（季度选择）](#zoehisquarterpicker)
- [ZoehisTimePicker（时间选择）](#zoehistimepicker)

---

## ZoehisDateBtn（日期选择）

### 概述
DatePicker 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisDateBtn from '@zoesoft.com.cn/his-component-vue/packages/datepicker';
Vue.use(ZoehisDateBtn);
```

### 基础用法
```vue
<zoehis-date-btn></zoehis-date-btn>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-date-btn>
</zoehis-date-btn>
```

### 常用事件使用
```vue
<template>
  <zoehis-date-btn @clickClearDate="handleEvent">
  </zoehis-date-btn>
</template>
<script>
export default {
  methods: {
    handleEvent(event) {
      console.log('clickClearDate 事件触发', event);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-date-btn ref="myRef"></zoehis-date-btn>
  <zoehis-button @clickenter="$refs.myRef.getActiveDate()">调用 getActiveDate 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| uiType | String | - | UI 类型 |
| btnArr | Array | - | - |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| clickClearDate | - | - |
| clickGetDate | - | - |
| computedBtnWidthFinish | - | - |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| getActiveDate | e | 获取相关数据 |
| clickClearDate | e | 点击清空 |
| clickGetDate | e | 点击确认 |
| hideOver | - | - |
| showOver | - | - |
| rangeBtnTextFn | e,column | 单日期 月末 |
| rangeBtnText | type | 单日期 '月末' |
| getLengthWidth | - | 获取相关数据 |
| showButtonMask | e | 小于0 不够放的情况下 |
| computedBtnWidth | - | 更新一次popper位置，如果popper再上方，第一次拿到的pop高度有问题 |

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisDateEditor（日期编辑）

### 概述
ZoehisDateEditor 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisDateEditor from '@zoesoft.com.cn/his-component-vue/packages/date-editor';
Vue.use(ZoehisDateEditor);
```

### 基础用法
```vue
<zoehis-date-editor></zoehis-date-editor>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-date-editor
  :disabled="true"
  format="yyyy-MM-dd HH:mm:ss">
</zoehis-date-editor>
```

### 常用事件使用
```vue
<template>
  <zoehis-date-editor @visible-change="handleEvent">
  </zoehis-date-editor>
</template>
<script>
export default {
  methods: {
    handleEvent(val) {
      console.log('visible-change 事件触发', val);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-date-editor ref="myRef"></zoehis-date-editor>
  <zoehis-button @clickenter="$refs.myRef.ProcessTimeFormat()">调用 ProcessTimeFormat 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| disabled | Boolean | - | 是否禁用 |
| value | - | - | 绑定值 |
| format | String | yyyy-MM-dd HH:mm:ss | 时间弹窗显示的格式 |
| valueFormat | String | - | v-model 值的格式 1.空"" 代表国际化格式  2.timestamp:时间戳  3.yyyy-MM-dd HH:mm:ss / yyyy-MM-dd 等 |
| inputformat | String | - | 输入框input 显示的格式 默认为空，与format一致 |
| mode | String | normal | 日期控件的模式（正常和显示范围） |
| initfocus | String | year | 显示弹窗时默认选中哪一个  year/month/date/hour/minute/seconds |
| endfocus | String | - | default:"seconds" |
| nowtime | Boolean | true | 若v-model为空时，弹窗是否要显示当前时间 默认 true显示 |
| autocheck | Boolean | true | 输入内容后 是否自动校验并跳转到下一个输入框 默认 true显示 |
| imeMode | String | disabled | 原始属性 ime-mode （输入法引擎） |
| width | Number / String | - | 文本的宽度 |
| uiType | String | - | 配置不同的ui样式  默认是带下划线的输入框，rectangle矩形带边框的输入框 |
| preventOverId | String | - | 容器 ID |
| icon | String | z_shijian_normal | 图标类名 |
| align | String | left | 弹窗对齐方式 |
| placeholder | String | - | 占位提示 |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| visible-change | val | 显隐状态变化时触发 |
| after-prev | - | - |
| focus | - | 获取焦点时触发 |
| tab-end | - | - |
| input | "",type | 值变化时触发 |
| change | "",type | 值改变时触发 |
| clear | - | 清空时触发 |
| save | "",type | - |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| ProcessTimeFormat | val | 处理时间格式 |
| clearValue | val,format | val 标准时间格式，清空format没有的日期,没有时分秒置为00,没有日月置为1，没有年默认当年 |
| afterPrevEvn | - | 逆向快捷键响应事件 ctrl+tab  /  ctrl+enter |
| focusEvn | - | - |
| boxBlurEvn | - | 弹窗焦点失去事件 |
| tabEndEvn | - | tab 到最后响应时的事件 |
| saveEvn | date,type | 相等 则代表没有改变 |
| showTimeErrorTip | startDate,endDate | 是否显示时间错误提示 |
| changeErrorTip | flag | 改变时间错误提示信息 供子组件调用 |
| focus | - | 外部调用的focus方法 |
| hide | - | 外部调用的hide 隐藏弹窗方法 |
| clearIcon | - | 清除 |

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisMonthPanel（月份选择）

### 概述
ZoehisMonthPicker 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisMonthPanel from '@zoesoft.com.cn/his-component-vue/packages/monthpicker';
Vue.use(ZoehisMonthPanel);
```

### 基础用法
```vue
<zoehis-month-panel></zoehis-month-panel>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-month-panel
  :isShowPanel="true">
</zoehis-month-panel>
```

### 常用事件使用
```vue
<template>
  <zoehis-month-panel @selectedDate="handleEvent">
  </zoehis-month-panel>
</template>
<script>
export default {
  methods: {
    handleEvent(val) {
      console.log('selectedDate 事件触发', val);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-month-panel ref="myRef"></zoehis-month-panel>
  <zoehis-button @clickenter="$refs.myRef.selectedMonth()">调用 selectedMonth 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| selectYearArr | Array | - | - |
| isShowPanel | Boolean | false | - |
| initDate | - | - | 单个时间 |
| uiType | String | - | UI 类型 |
| align | String | - | 对齐 |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| selectedDate | dateJson | - |
| clickGetDate | this.getOneDate | - |
| clickClearDate | this.isClearOneDateJson | - |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| selectedMonth | item,e | 增加一个属性用来判断是点击表盘中上或下个月日期改变的月份 |
| changeYear | val | 切换年份 |
| clickClearDate | e | 清除 |
| addYearJsonToArr | yearArrs | 初始化年份过大或过小 |
| updateInfo | date,dateStamp | 更新弹窗里的信息 |
| prevYear | - | 上一年按钮 |
| nextYear | - | 下一年按钮 |
| yearScrollTop | - | 此时选中行应该为第五行 |
| yearScrollBottom | - | item.text += this.i18nAttr.years; |
| dropdownFocusin | - | 日期下拉的focusin事件 |
| dropdownFocusout | - | 日期下拉的focusout事件 |
| listenDomAttr | - | 监听节点属性的变化 主要监听x-placement的值的变化 解决错误提示跟弹窗叠在一起的bug |

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisNewDatePanel（新版日期选择）

### 概述
DatePicker 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisNewDatePanel from '@zoesoft.com.cn/his-component-vue/packages/newDatepicker';
Vue.use(ZoehisNewDatePanel);
```

### 基础用法
```vue
<zoehis-new-date-panel></zoehis-new-date-panel>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-new-date-panel>
</zoehis-new-date-panel>
```

### 常用事件使用
```vue
<template>
  <zoehis-new-date-panel @timeValue="handleEvent">
  </zoehis-new-date-panel>
</template>
<script>
export default {
  methods: {
    handleEvent(val) {
      console.log('timeValue 事件触发', val);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-new-date-panel ref="myRef"></zoehis-new-date-panel>
  <zoehis-button @clickenter="$refs.myRef.setDate()">调用 setDate 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| startDateObject | Object | - | - |
| endDateObject | Object | - | - |
| selectedDateObject | Object | - | - |
| showFooterFlag | Boolean | true | - |
| selectYearArr | Array | - | - |
| isShowPanel | Boolean | false | - |
| dateInitFlag | Boolean | true | 输入框为空值，显示日期组件时默认 选中今天 |
| initDate | - | - | 单个时间   或开始时间 |
| isRightPanel | Boolean | false | 是否是右边表盘 |
| initEndDate | - | - | - |
| startTimestampOfIndex | Number | - | - |
| isClickStartDate | Boolean | - | - |
| initDateHelpSave | - | - | - |
| align | String | - | 对齐 |
| format | String | - | 显示格式 |
| imeMode | String | disabled | 原始属性 ime-mode （输入法引擎） |
| initStartTime | Object | - | - |
| initEndTime | Object | - | - |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| timeValue | timeJson | - |
| dateValue | val | - |
| theYearMonthChange | this.theYear, val, this.isRightPanel | - |
| enter | - | 按下回车时触发 |
| dateInputBlur | dateValue, this.isRightPanel | - |
| getMouseOverStamp | mouseStamp | - |
| clickGetDate | this.getOneDate | - |
| clickClearDate | this.isClearOneDateJson | - |
| selectedDate | dateJson,this.isRightPanel,{selectedTimestamp, minTimestamp ,maxTimestamp} | - |
| click-range-btn | fnName | - |
| click-date-btn | fnName, { year: this.theYear, month: this.theMonth } | - |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| handleSkipAction | curInfo | 内部事件处理 |
| handleEnter | - | 内部事件处理 |
| handleDateInputChange | newDate | this.$refs.oneTable.clickSelectedDate(date.getDate(), this.theMonth, this.theYear, 2, date.getTime(), true); |
| setDate | selectedTimestamp, minTimestamp, maxTimestamp | maxTimestamp = maxTimestamp.setHours(theTime.getHours(), theTime.getMinutes(), theTime.getSeconds()); |
| handleDateInputKeyup | date | }) |
| setTheDateInputVal | date | 设置相关数据 |
| setDateInputVal | - | 设置相关数据 |
| handleDateInputBlur | dateValue | 失焦 处理手动输入日期输入框数据 |
| setYearMonth | year, month | 年月控制 |
| setRangeTime | selectedTimestamp, minTimestamp, maxTimestamp | 联动手动日期输入框 |
| updateInfo | date,dateStamp | 更新弹窗里的信息 |
| dealYearArr | - | 比当前最小值小 或者比当前的最大值大  或者 下拉的长度大于50（数据太多，影响体验） |
| isDate | date | 判断是否是 日期格式 |
| handleClickTimePicker | - | 时分秒组件-点击确认 |
| dropdownFocusin | - | 日期下拉的focusin事件 |
| dropdownFocusout | - | 日期下拉的focusout事件 |
| listenDomAttr | - | 监听节点属性的变化 主要监听x-placement的值的变化 解决错误提示跟弹窗叠在一起的bug |
| getActiveDate | - | 获取相关数据 |
| getMouseOverStamp | mouseStamp | 获取相关数据 |
| clickGetDate | - | 有选中面板日期 |
| clickClearDate | - | 点击清空 |
| changeClear | boo | - |
| addYearJsonToArr | yearArrs | - |
| m_prevMonth | - | 月份往后退的情况(多个函数调用的函数以m_开头) |
| m_nextMonth | - | 月份往前进的情况(多个函数调用的函数以m_开头) |
| yearScrollTop | - | 此时选中行应该为第五行 |
| yearScrollBottom | - | 向下滚动加载年份 |
| getDateToParent | date, {selectedTimestamp, minTimestamp ,maxTimestamp} | 增加一个属性用来判断是点击表盘中上或下个月日期改变的月份 |
| timeEnterEn | - | } |
| prevYear | - | 上一年按钮 |
| prevMonth | - | 上一个月按钮 |
| nextYear | - | 下一年按钮 |
| nextMonth | - | 下一个月按钮 |
| eStop | e | 点击表盘阻止冒泡 |
| clickRangeBtnEn | fnName | - |
| clickDateBtnEn | fnName | - |
| setTheTime | val | 设置相关数据 |

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisQuarterPicker（季度选择）

### 概述
ZoehisQuarterPicker 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisQuarterPicker from '@zoesoft.com.cn/his-component-vue/packages/quarter-picker';
Vue.use(ZoehisQuarterPicker);
```

### 基础用法
```vue
<zoehis-quarter-picker></zoehis-quarter-picker>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-quarter-picker>
</zoehis-quarter-picker>
```

### 常用事件使用
```vue
<template>
  <zoehis-quarter-picker @hide="handleEvent">
  </zoehis-quarter-picker>
</template>
<script>
export default {
  methods: {
    handleEvent(event) {
      console.log('hide 事件触发', event);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-quarter-picker ref="myRef"></zoehis-quarter-picker>
  <zoehis-button @clickenter="$refs.myRef.setCurrentFocus()">调用 setCurrentFocus 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| beforeClick | Function | - | 点击前回调 |
| disabledDateFun | Function | - | - |
| enabledRange | Object | - | - |
| type | String | date | 类型 |
| align | String | left | 对齐 |
| disabled | Boolean | false | 是否禁用 |
| preventOverId | String | - | 容器 ID |
| value | Object / Array | - | 绑定值 |
| clearable | Boolean | true | 可取消选中 |
| readonly | Boolean | false | 是否只读 |
| errorTip | String | - | 错误的提示内容 为 "" 时表示关闭错误的提示 |
| icon | String | z_time_normal | 图标类名 |
| showErrorTip | Boolean | false | 为true， 显示错误提示 |
| imeMode | String | disabled | 原始属性 ime-mode （输入法引擎） |
| minErrorWidth | [String,Number] | - | 最小错误宽 |
| uiType | String | - | 配置不同的ui样式  默认是带下划线的输入框，rectangle矩形带边框的输入框 |
| placeholder | String | 选择季度 | 占位提示 |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| hide | - | - |
| show | - | - |
| focus | - | 获取焦点时触发 |
| input | {} | 值变化时触发 |
| change | {} | 值改变时触发 |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| setCurrentFocus | num | 设置相关数据 |
| setErrorTipPlace | placement | bottom、top互换 |
| changeClear | boo | - |
| unEqualEmit | val | - |
| inputBlur | - | this.keyEnterHandle(); |
| getDisabled | - | 获取是否禁用 |
| focus | - | 手动聚焦 |
| selectAllInput | - | - |
| focusInput | - | - |
| getText | activeDate | 获取文本 |
| getShowInputText | val | val:  {years: '2022', quarter: 'Q1'} -> 2022年第一季度 |
| clear | - | 清空数据 |
| clickClearDate | - | 清空后初始化年、月份-- |
| isSameDate | firstTime,secondTime | - |
| initFunc | d | this.changeClear(false); |
| setDate | chooseDate | 判断时间是否相等 |
| doSave | value | - |
| clickInput | e | nextTick 解决第一次点击焦点定位问题 |
| clickIcon | - | 点击icon |
| getSelectMonth | val = this.value | 获得所选季度的全部月份 供外部调用 |
| formatTwoDigitNumber | number | 如果字符串的长度小于 2，就在前面添加一个零 |
| isEqualDate | equalList | - |
| checkoutWrite | keyCode | 单纯 date 的情况 |
| eStop | e | 点击表盘阻止冒泡 |
| changePanel | isShow | 改变下拉框显示状态 |
| setTime | date | 设置相关数据 |
| show | - | 显示弹窗 |
| hide | - | 隐藏弹窗 |

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisTimePicker（时间选择）

### 概述
ZoehisTimePicker 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisTimePicker from '@zoesoft.com.cn/his-component-vue/packages/time-picker';
Vue.use(ZoehisTimePicker);
```

### 基础用法
```vue
<zoehis-time-picker></zoehis-time-picker>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-time-picker
  :showSeconds="true"
  hours="0"
  minutes="0">
</zoehis-time-picker>
```

### 常用事件使用
```vue
<template>
  <zoehis-time-picker @change="handleEvent">
  </zoehis-time-picker>
</template>
<script>
export default {
  methods: {
    handleEvent(val) {
      console.log('change 事件触发', val);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-time-picker ref="myRef"></zoehis-time-picker>
  <zoehis-button @clickenter="$refs.myRef.emitChange()">调用 emitChange 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| showSeconds | Boolean | true | 是否显示秒 |
| hours | Number | 0 | 当前选中的小时 |
| minutes | Number | 0 | 当前选中的分 |
| seconds | Number | 0 | 当前选中的秒 |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| change | obj | 值改变时触发 |
| select-range | 0,2 | - |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| emitChange | obj | - |
| handleScrollEve | type | 内部事件处理 |
| handleClick | type, value | 点击事件 |
| emitSelectRange | type | 全选输入框的某一段 |
| ajustScrollTop | - | 重置选中值的滚动条位置 |
| initScrollPos | - | 初始化滚动条的位置 |
| ajustZoehisTop | type, value | 将滚动条滚动到选中值的位置上 |

### Slots（插槽）
该组件未定义插槽。

---

