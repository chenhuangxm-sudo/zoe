# 基础组件

## 目录

- [ZoehisBadge（角标）](#zoehisbadge)
- [ZoehisButton（按钮）](#zoehisbutton)
- [ZoehisDivider（分割线）](#zoehisdivider)
- [ZoehisSpinner（加载中）](#zoehisspinner)
- [ZoehisTag（标签）](#zoehistag)

---

## ZoehisBadge（角标）

### 概述
ZoehisBadge 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisBadge from '@zoesoft.com.cn/his-component-vue/packages/badge';
Vue.use(ZoehisBadge);
```

### 基础用法
```vue
<zoehis-badge></zoehis-badge>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-badge
  type="primary">
</zoehis-badge>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| value | String / Number | - | 显示值 |
| type | String | primary | 类型 primary / info / warning |
| max | String / Number | - | 最大值，超过最大值会显示 '{max}+' |

### ② 事件说明（Events）
该组件未触发自定义事件。

### ③ 方法说明（Methods）
该组件未定义方法。

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisButton（按钮）

### 概述
ZoehisButton 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisButton from '@zoesoft.com.cn/his-component-vue/packages/button';
Vue.use(ZoehisButton);
```

### 基础用法
```vue
<zoehis-button></zoehis-button>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-button
  type="default"
  size="normal"
  :disabled="true">
</zoehis-button>
```

### 常用事件使用
```vue
<template>
  <zoehis-button @clickenter="handleEvent">
  </zoehis-button>
</template>
<script>
export default {
  methods: {
    handleEvent(val) {
      console.log('clickenter 事件触发', val);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-button ref="myRef"></zoehis-button>
  <zoehis-button @clickenter="$refs.myRef.focus()">调用 focus 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| type | String | default | 按钮类型  default/primary/features/plain/plainPrimary/delete/clearCard(默认的、主要的、底部按钮功能键、无图标圆角略小按钮、无图标圆角略小主要按钮、删除按钮、清卡按钮) |
| size | String | normal | normal | small | big（正常按钮/小型按钮/大型按钮） |
| disabled | Boolean | - | 是否禁用 |
| shrinkLevel | Number / String | 1 | 值越高，就越先被收起来 |
| width | Number / String | - | 按钮的宽度 |
| label | Number / String | - | 标签文本 |
| sortNo | Number | - | - |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| clickenter | evt | 点击按钮时触发 |
| focus | evt | 获取焦点时触发 |
| blur | evt | 失去焦点时触发 |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| handleClickEnter | evt | 内部事件处理 |
| handleFocus | evt | 内部事件处理 |
| handleBlur | evt | 内部事件处理 |
| focus | evt | 焦点聚集的方法 |
| blur | - | 手动失去焦点 |
| getDisabled | - | 获取当前按钮是否不可以点击的状态  true代表不可以点击 |

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisDivider（分割线）

### 概述
divider

### 引入方式
```js
import Vue from 'vue';
import ZoehisDivider from '@zoesoft.com.cn/his-component-vue/packages/divider';
Vue.use(ZoehisDivider);
```

### 基础用法
```vue
<zoehis-divider></zoehis-divider>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-divider
  direction="horizontal"
  position="left">
</zoehis-divider>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| direction | String | horizontal | horizontal: 横向  vertical: 竖向 |
| position | String | left | 插槽位置 |

### ② 事件说明（Events）
该组件未触发自定义事件。

### ③ 方法说明（Methods）
该组件未定义方法。

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisSpinner（加载中）

### 概述
ZoehisSpinner 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisSpinner from '@zoesoft.com.cn/his-component-vue/packages/spinner';
Vue.use(ZoehisSpinner);
```

### 基础用法
```vue
<zoehis-spinner></zoehis-spinner>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-spinner
  percentage="0"
  :error="true">
</zoehis-spinner>
```

### 常用方法使用
```vue
<template>
  <zoehis-spinner ref="myRef"></zoehis-spinner>
  <zoehis-button @clickenter="$refs.myRef.changeColor()">调用 changeColor 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| percentage | Number / String | 0 | 百分比 |
| error | Boolean | false | 错误状态 |
| type | String | - | 类型 'loading' |
| color | String | - | 颜色 |
| theme | String | white | 主题 |

### ② 事件说明（Events）
该组件未触发自定义事件。

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| changeColor | - | - |

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisTag（标签）

### 概述
ZoehisTag 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisTag from '@zoesoft.com.cn/his-component-vue/packages/tag';
Vue.use(ZoehisTag);
```

### 基础用法
```vue
<zoehis-tag></zoehis-tag>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-tag
  type="plain"
  theme="default">
</zoehis-tag>
```

### 常用方法使用
```vue
<template>
  <zoehis-tag ref="myRef"></zoehis-tag>
  <zoehis-button @clickenter="$refs.myRef.tagClickEn()">调用 tagClickEn 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| type | String | plain | 类型 |
| theme | String | default | 主题 |
| customClass | String | - | 自定义类名 |
| color | String | - | 颜色 |
| backgroundColor | String | - | 背景色 |
| closable | Boolean | false | 可关闭 |
| data | Object | - | 附加数据 |
| title | String / Number | - | 标题 |
| code | String / Number | - | 编码 |
| tagCode | String | code | 标签编码字段 |
| tagTitle | String | title | 标签标题字段 |

### ② 事件说明（Events）
该组件未触发自定义事件。

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| tagClickEn | e | - |
| closeClickEn | e | - |
| remove | - | 移除 |

### Slots（插槽）
该组件未定义插槽。

---

