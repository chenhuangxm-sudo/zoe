# 其他组件

## 目录

- [ZoehisBackTop（回到顶部）](#zoehisbacktop)
- [ZoehisCascader（级联选择）](#zoehiscascader)
- [ZoehisCollapse（折叠面板）](#zoehiscollapse)
- [ZoehisDraggableResizable（拖拽缩放）](#zoehisdraggableresizable)
- [ZoehisForm（表单）](#zoehisform)
- [ZoehisFunc（功能）](#zoehisfunc)
- [ZoehisImgPreview（图片预览）](#zoehisimgpreview)
- [ZoehisTransfer（穿梭框）](#zoehistransfer)

---

## ZoehisBackTop（回到顶部）

### 概述
ZoehisBackTop 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisBackTop from '@zoesoft.com.cn/his-component-vue/packages/backTop';
Vue.use(ZoehisBackTop);
```

### 基础用法
```vue
<zoehis-back-top></zoehis-back-top>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-back-top
  visibilityHeight="0"
  target="html"
  right="79">
</zoehis-back-top>
```

### 常用事件使用
```vue
<template>
  <zoehis-back-top @topClick="handleEvent">
  </zoehis-back-top>
</template>
<script>
export default {
  methods: {
    handleEvent(val) {
      console.log('topClick 事件触发', val);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-back-top ref="myRef"></zoehis-back-top>
  <zoehis-button @clickenter="$refs.myRef.init()">调用 init 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| visibilityHeight | Number | 0 | 滚动高度达到此参数值才出现，为0一直存在，没开 |
| target | String | html | 触发滚动的对象 |
| right | String | 79 | 控制其显示位置, 距离页面右边距 |
| bottom | String | 116 | 控制其显示位置，距离页面底部距离 |
| isDown | Boolean | true | 是否显示回到底部 |
| position | String | absolute | 控制组件是否根据target定位，fixed/absolute |
| zIndex | String | 100 | 自定义层级 |
| customClass | String | - | 自定义类名 |
| lightClass | String | zoehis_backtop_light | 自定义触顶或触底高亮类名 |
| isRAF | Boolean | true | 判断是否使用动画效果 |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| topClick | e | - |
| bottomClick | e | - |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| init | - | 判断是否显示回到底部 |
| onScroll | - | this.visible = scrollTop >= this.visibilityHeight; |
| topClick | e | 点击按钮触发的事件 |
| bottomClick | e | - |
| scrollToTop | - | 超过 500 毫秒直接返回页面顶部 |
| scrollToBottom | - | 超过 500 毫秒直接返回页面底部 |

### Slots（插槽）
| 插槽名 | 作用域参数 | 说明 |
|--------|-----------|------|
| top | - |  |
| bottom | - |  |

---

## ZoehisCascader（级联选择）

### 概述
ZoehisCascader 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisCascader from '@zoesoft.com.cn/his-component-vue/packages/cascader';
Vue.use(ZoehisCascader);
```

### 基础用法
```vue
<zoehis-cascader></zoehis-cascader>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-cascader
  placement="bottom-start">
</zoehis-cascader>
```

### 常用事件使用
```vue
<template>
  <zoehis-cascader @input="handleEvent">
  </zoehis-cascader>
</template>
<script>
export default {
  methods: {
    handleEvent(val) {
      console.log('input 事件触发', val);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-cascader ref="myRef"></zoehis-cascader>
  <zoehis-button @clickenter="$refs.myRef.initI18n()">调用 initI18n 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| placement | String | bottom-start | 位置 |
| offsetXY | Array | - | - |
| value | - | - | 绑定值 |
| options | Array | - | 选项数据 |
| configs | Object | - | 配置项 |
| lazy | Boolean | false | 是否懒加载 |
| lazyLoad | Function | - | - |
| uiType | String | - | 类型 默认: 下划线， rectangle: 矩形 |
| width | String / Number | 160 | 宽度 |
| placeholder | String | - | 占位符 |
| disabled | Boolean | false | 是否禁用 |
| separator | String | / | 选项内路径分隔符 |
| limit | String | - | 各个选项间分割符（多选适用） |
| showAllLevels | Boolean | true | 文本是否完整路径 |
| iconFont | String | - | 自定义下拉图标 |
| clearable | Boolean | true | 清空标志 |
| filterable | Boolean | false | 是否有搜索功能 |
| emptyText | String | - | 空提示文本 |
| debounce | Number | 200 | 防抖 |
| filterMethod | Function | - | 自定义过滤函数 |
| beforeFilter | Function | - | 过滤前方法 |
| align | String | left | 弹窗对齐方式 |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| input | val | 值变化时触发 |
| change | val, this.getCheckedNodes( | 值改变时触发 |
| visible-change | true | 显隐状态变化时触发 |
| active-item-change | node | 激活项变化时触发 |
| expand-change | node | 展开状态变化时触发 |
| clear | - | 清空时触发 |
| blur | e | 失去焦点时触发 |
| focus | e | 获取焦点时触发 |
| scroll-after | e, node | 滚动后触发 |
| scroll-to-bottom | e, node | 滚动到底部时触发 |
| scroll-to-top | e, node | 滚动到顶部时触发 |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| initI18n | - | 初始化 |
| setWidthFilter | - | 设置过滤弹窗最小宽度 |
| computedPresentContent | - | - |
| computedMultiplePresentText | - | - |
| computedPresentText | - | - |
| getFilterNodeList | - | } |
| handleInput | val, e | 有值 |
| handleCheckboxChange | event, node | 内部事件处理 |
| handleNodeClick | node | node.checked = true; |
| handlePopHide | - | this.inputVal = this.presentText; |
| handlePopShow | - | this.$emit("visible-change", true); |
| handleExpandChange | node | this.$emit("active-item-change", node); |
| handleKeyDown | e | 内部事件处理 |
| focusFirstNode | - | 搜索面板 |
| handleFilterKeyDown | e | 内部事件处理 |
| toggelePanelVisible | visible | - |
| handleClickInputIcon | - | 内部事件处理 |
| getCheckedNodes | leafOnly | 获取选中节点 |
| panelChange | checkedNodes, value | this.$emit("change", checkedNodes, value); |
| clear | - | 清空数据 |
| handleInputBlur | e | 内部事件处理 |
| handleInputFocus | e | 内部事件处理 |
| handelActiveItemChange | node | - |
| appendNode | pKey, newNodeData | 追加节点 |
| clearCheckedNodes | - | 清除 |
| getCurrentCheckedNodes | - | 获取相关数据 |
| removeNode | nodeKey | 移除节点 |
| setNodeStatusByKey | key, val | 设置相关数据 |
| setValueStatusByValue | value, val | 设置相关数据 |
| handleElClick | - | 内部事件处理 |
| scrollafter | e, node | - |
| scrollToBottom | e, node | - |
| scrollToTop | e, node | - |

### Slots（插槽）
| 插槽名 | 作用域参数 | 说明 |
|--------|-----------|------|
| default | - |  |
| searchNode | - |  |
| empty | - |  |

---

## ZoehisCollapse（折叠面板）

### 概述
ZoehisCollapse 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisCollapse from '@zoesoft.com.cn/his-component-vue/packages/collapse';
Vue.use(ZoehisCollapse);
```

### 基础用法
```vue
<zoehis-collapse></zoehis-collapse>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-collapse
  :accordion="true"
  :border="true">
</zoehis-collapse>
```

### 常用事件使用
```vue
<template>
  <zoehis-collapse @input="handleEvent">
  </zoehis-collapse>
</template>
<script>
export default {
  methods: {
    handleEvent(val) {
      console.log('input 事件触发', val);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-collapse ref="myRef"></zoehis-collapse>
  <zoehis-button @clickenter="$refs.myRef.setActiveNames()">调用 setActiveNames 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| accordion | Boolean | - | 是否手风琴模式 |
| value | Array / String / Number | - | 绑定值 |
| border | Boolean | true | 边框 |
| uiType | String | - | UI 类型 |
| indent | String | - | - |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| input | value | 值变化时触发 |
| change | value | 值改变时触发 |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| setActiveNames | activeNames | 设置相关数据 |
| handleItemClick | item | 拷贝新一份 |

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisDraggableResizable（拖拽缩放）

### 概述
ZoehisDraggableResizable 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisDraggableResizable from '@zoesoft.com.cn/his-component-vue/packages/draggable-resizable';
Vue.use(ZoehisDraggableResizable);
```

### 基础用法
```vue
<zoehis-draggable-resizable></zoehis-draggable-resizable>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-draggable-resizable
  :active="true"
  :draggable="true"
  :resizable="true">
</zoehis-draggable-resizable>
```

### 常用事件使用
```vue
<template>
  <zoehis-draggable-resizable @activated="handleEvent">
  </zoehis-draggable-resizable>
</template>
<script>
export default {
  methods: {
    handleEvent(val) {
      console.log('activated 事件触发', val);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-draggable-resizable ref="myRef"></zoehis-draggable-resizable>
  <zoehis-button @clickenter="$refs.myRef.reviewDimensions()">调用 reviewDimensions 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| active | Boolean | false | 当前激活步骤 |
| draggable | Boolean | true | 可拖拽 |
| resizable | Boolean | true | 可缩放 |
| w | Number | 200 | - |
| h | Number | 200 | - |
| minw | Number | 50 | - |
| minh | Number | 50 | - |
| x | Number | 0 | - |
| y | Number | 0 | - |
| z |  String / Number | auto | - |
| handles | Array | - | - |
| axis | String | both | - |
| grid | Array | - | - |
| parent | Boolean | false | - |
| maximize | Boolean | false | - |
| updatepos | Boolean | false | - |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| activated | this | - |
| update:active | true | - |
| deactivated | - | - |
| resizing | this | - |
| dragging | this | - |
| resizestop | this | - |
| dragstop | this | - |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| reviewDimensions | - | - |
| elmDown | e | 点击的对象在组件的内部 则触发相应的 可以在这边控制哪些内容不能拖拽 |
| deselect | e | }); |
| handleDown | handle, e | 内部事件处理 |
| fillParent | e | - |
| handleMove | e | 内部事件处理 |
| handleUp | e | 内部事件处理 |
| changePos | left,top | 改变位置 |

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisForm（表单）

### 概述
form

### 引入方式
```js
import Vue from 'vue';
import ZoehisForm from '@zoesoft.com.cn/his-component-vue/packages/form';
Vue.use(ZoehisForm);
```

### 基础用法
```vue
<zoehis-form></zoehis-form>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-form
  type="single">
</zoehis-form>
```

### 常用方法使用
```vue
<template>
  <zoehis-form ref="myRef"></zoehis-form>
  <zoehis-button @clickenter="$refs.myRef.preventFunc()">调用 preventFunc 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| data | Object | - | 表单数据 |
| type | String | single | 类型 |
| rules | Object | - | 表单规则 |
| gutter | Number | 30 | 栅格间隔 |
| labelWidth | String | 50px | 标签宽度 |
| disabled | Boolean | false | 是否禁用 |
| showMessage | Boolean | true | 是否展示信息 |
| span | String / Number | - | 表单项所占栅栏数目 |
| offset | Number | - | 偏移量 |
| gutterSide | Boolean | true | - |
| labelPosition | String | left | 标签位置 |
| labelWidthFixed | Boolean | false | 是否强制固定文本标签宽度（不建议使用） |
| isRows | Boolean | false | 是否采用嵌套row col 不建议使用 |
| space | Number / String | - | 表单项上下间距 |
| labelStyle | Object | - | - |

### ② 事件说明（Events）
该组件未触发自定义事件。

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| preventFunc | e | - |
| resetForm | - | 重置 |
| clearValidate | props | 清除校验状态 |
| validate | callback, rules={}, isBefore = false, isOriginalValidate = true | 最后一个表单项 结果回调 |
| validateField | props, callback | 过滤校验的表单项 |

### Slots（插槽）
| 插槽名 | 作用域参数 | 说明 |
|--------|-----------|------|
| default | - |  |

---

## ZoehisFunc（功能）

### 概述
ZoehisFunc 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisFunc from '@zoesoft.com.cn/his-component-vue/packages/func';
Vue.use(ZoehisFunc);
```

### 基础用法
```vue
<zoehis-func></zoehis-func>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-func
  excelVersion="default">
</zoehis-func>
```

### 常用方法使用
```vue
<template>
  <zoehis-func ref="myRef"></zoehis-func>
  <zoehis-button @clickenter="$refs.myRef.tableToExcel()">调用 tableToExcel 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| excelVersion | String | default | 兼容版（2003）：compatible, 默认（2007）：default |

### ② 事件说明（Events）
该组件未触发自定义事件。

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| tableToExcel | excelData,fileName="导出",worksheet="Sheet1",tableConfig=[] | 2007+版本 xlsx |

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisImgPreview（图片预览）

### 概述
ZoehisImgPreview 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisImgPreview from '@zoesoft.com.cn/his-component-vue/packages/img-preview';
Vue.use(ZoehisImgPreview);
```

### 基础用法
```vue
<zoehis-img-preview></zoehis-img-preview>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-img-preview>
</zoehis-img-preview>
```

### 常用事件使用
```vue
<template>
  <zoehis-img-preview @close="handleEvent">
  </zoehis-img-preview>
</template>
<script>
export default {
  methods: {
    handleEvent(val) {
      console.log('close 事件触发', val);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-img-preview ref="myRef"></zoehis-img-preview>
  <zoehis-button @clickenter="$refs.myRef.resizeBox()">调用 resizeBox 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| imgList | Array | - | 预览图片列表 |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| close | data | 关闭时触发 |
| show | data | - |
| change | data, oldData, type | 值改变时触发 |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| resizeBox | - | - |
| show | - | 显示 |
| close | - | 关闭 |
| prev | - | - |
| next | - | - |
| getCurrentImgData | - | 获取相关数据 |
| setActiveItem | uid | 设置相关数据 |
| closeFn | data | - |
| showFn | data | - |
| imgChange | data, oldData, type | - |

### Slots（插槽）
该组件未定义插槽。

---

## ZoehisTransfer（穿梭框）

### 概述
ZoehisTransfer 组件

### 引入方式
```js
import Vue from 'vue';
import ZoehisTransfer from '@zoesoft.com.cn/his-component-vue/packages/transfer';
Vue.use(ZoehisTransfer);
```

### 基础用法
```vue
<zoehis-transfer></zoehis-transfer>
```

### 常用属性配置
```vue
<!-- 请根据实际需求配置属性 -->
<zoehis-transfer>
</zoehis-transfer>
```

### 常用事件使用
```vue
<template>
  <zoehis-transfer @search-input="handleEvent">
  </zoehis-transfer>
</template>
<script>
export default {
  methods: {
    handleEvent(val) {
      console.log('search-input 事件触发', val);
    }
  }
}
</script>
```

### 常用方法使用
```vue
<template>
  <zoehis-transfer ref="myRef"></zoehis-transfer>
  <zoehis-button @clickenter="$refs.myRef.inputNativeEn()">调用 inputNativeEn 方法</zoehis-button>
</template>
```

### ① 属性说明（Props）
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| searchParam | Object | - | 搜索参数 |
| leftWidth | String | - | 宽度 |
| rightWidth | String | - | 右侧宽度 |

### ② 事件说明（Events）
| 事件名 | 参数 | 说明 |
|--------|------|------|
| search-input | val | - |
| search-change | val | - |
| search-clickicon | this.searchVal,e | - |
| right-click | e | - |
| left-click | e | - |

### ③ 方法说明（Methods）
| 方法名 | 参数 | 说明 |
|--------|------|------|
| inputNativeEn | val | - |
| inputChangeEn | val | - |
| inputClickIconEn | e | - |
| toRightClickEn | e | - |
| toLeftClickEn | e | - |
| setSearchValue | val='' | 设置相关数据 |
| searchFocus | - | - |

### Slots（插槽）
| 插槽名 | 作用域参数 | 说明 |
|--------|-----------|------|
| transferSearch | - |  |
| leftTitle | - |  |
| leftMain | - |  |
| rightTitle | - |  |
| rightMain | - |  |

---

